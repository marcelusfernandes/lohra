# Lohra — estado do projeto (histórico por fase e por wave)

Este arquivo é a memória narrativa do projeto: o que cada Fase/Wave entregou, o que foi validado ao vivo, o que ficou
pendente com custo nomeado, e o que vem a seguir. Instruções de trabalho ficam em `CLAUDE.md`; specs em `docs/specs/`;
decisões e evidência por data em `docs/history/`. Atualize a seção da rodada e o **Próximo** a cada release.

## Estado atual: Wave 10.2 entregue · run autônoma #108 em andamento
Tags git por fase: `phase-0`..`phase-5`. Pacote na versão 0.0.27; as correções posteriores da main ainda não foram publicadas no PyPI. A run [#108](https://github.com/marcelusfernandes/lohra/issues/108) continua escolhendo trabalhos executáveis após cada entrega, com CI e revisão independente por SHA. As medições históricas abaixo mantêm suas datas e limites; testes com clientes sintéticos não renovam evidência ao vivo.
- **0** esqueleto & contratos · **1** agent core (chat + streaming + CLI `lohra chat`) · **2** tools (fs/terminal), approval gate, SessionDB · **3** gateway FastAPI WS+REST + app desktop Tauri/React · **4** memory + skills (self-improving): MemoryStore, SKILL.md, SOUL.md, session_search (FTS5), snapshot frozen.
- **5 (completa):** metade A — ContextCompressor + AuxClient + compactação preflight no loop + lineage split no `lohra chat`. Metade B — `delegate_task` (subagents isolados: contexto fresco, caps pai=90/filho=50, depth 1, concorrência 3, auto-deny no filho; `lohra/agent/delegate.py`). Lineage split no gateway (evento `session.forked`, filha reusa Agent, pai despejado/bloqueado, busy-lock compartilhado; frontend faz rebind do `$sessionId`) + `compression_locks` (lock cross-process na tabela SQLite, PK single-winner + lease TTL, `busy_timeout` + back-off WAL).
- **6 (em andamento):** **Mais providers** ✅ — transport `chat_completions` (protocolo OpenAI; 11 providers: anthropic, openai, openrouter, deepseek, groq, together, gemini [endpoint OpenAI-compat do Google], xai (grok), glm (zhipu), kimi (moonshot), ollama [keyless via `requires_api_key=False`]) + `OpenAIClient` + `build_client(profile)` + CLI/gateway genéricos. **MCP client** ✅ — conecta servidores MCP (`~/.lohra/mcp.json`), expõe tools como `mcp_{server}_{tool}` no registry; ponte sync↔async, stdio+http (dispatcher `connect_session`). `lohra/mcp/`. **Servidor OpenAI-compatível** ✅ — `lohra serve`: `POST /v1/chat/completions` + `POST /v1/responses` (Responses API; objetos/eventos validados contra o SDK `openai` real), `/v1/models`, `/health`, auth Bearer; modo relay (default) + **modo agêntico** (`--tools <allow-list>`, allow-list gateia execução). `lohra/server/`. **Cron / jobs agendados** ✅ — `~/.lohra/cron/jobs.json` + matcher próprio (once/interval/cron) + scheduler em thread (dashboard) + tool `cronjob` interceptada + `lohra cron <…>`; jobs rodam como agente fresco sem tools, persistem sessão `source="cron"`. `lohra/cron/`. **Terminal embutido no desktop** ✅ — `portable-pty` (Rust) spawna o shell num PTY, comandos `pty_open/write/resize/close` + eventos `pty-output`/`pty-exit`; `TerminalPanel` (xterm.js) com toggle no header. `desktop/src-tauri/src/pty.rs`, `desktop/src/Terminal.tsx`. **Vision** ✅ — tool `vision_analyze` interceptada: imagem (`path` local→data URI base64 / `url` remota) + prompt opcional → análise textual de um modelo vision-capable (`make_vision_runner`, one-shot). Formato canônico = parts OpenAI (`image_url`), convertido pelos transports (chat_completions passa as parts; anthropic_messages traduz `image_url`→bloco `image`). Wirada no CLI/dashboard, excluída de subagentes/server. `lohra/vision/`. **Image_gen** ✅ — tool `image_gen` interceptada (prompt + `size`/`n` opcionais) → gera via OpenAI Images API (gpt-image-1), salva PNGs em `~/.lohra/images/` e devolve os paths. Capacidade `generate_image` no `ModelClient` (base levanta erro limpo; `OpenAIClient` chama `images.generate`, decodifica `b64_json`); `size` enum (só tamanhos válidos do gpt-image-1), `n` clampado 1–10. Só funciona no provider OpenAI (Images API). `lohra/imagegen/`. **Browser tools** ✅ (search + fetch) — `web_fetch` (httpx → texto legível via `html.parser`, recusa não-texto) + `web_search` (backend plugável `SearchBackend`, default keyless DuckDuckGo; distingue `SearchUnavailable` de zero resultados). Tools **normais** (stateless, como fs/terminal) → rodam em subagentes e gateáveis pela allow-list do server. **Guard SSRF na camada de fetch** (`validate_public_url`: só http(s), recusa loopback/privado/link-local/metadata/IPv4-mapped; redirects à mão revalidando cada hop; cap de bytes/chars). `lohra/web/`. **Profiles isolados** ✅ — `LOHRA_PROFILE` (ou `lohra --profile <nome>`) re-rooteia **todo** o estado sob `~/.lohra/profiles/<nome>/` (memória, skills, sessões/state.db, cron, mcp.json, imagens). Fonte única: `lohra_home()` resolve o profile (todo subsistema já passa por ela → isola por construção); `lohra_base()` é o root independente. Sem profile → exatamente `~/.lohra` de hoje (backward-compat, nada migrado). Validação anti-traversal (`validate_profile_name`) enforçada no read de `LOHRA_PROFILE` + na flag. CLI: `--profile` + `lohra profile list|create`. `lohra/memory/paths.py`. **Self-update (backend git-pull)** ✅ — `lohra update`: resolve o repo pela localização do pacote (`lohra.__file__`, não o CWD), pré-checks de estado de 1ª classe (tree sujo→aborta, sem upstream/detached, diverged≠erro, up-to-date) + `git pull --ff-only`; reporta arquivos + restart-to-apply; `pyproject` mudou→dica `pip install -e backend` (remédio que funciona, não re-run). `--check` (fetch+behind) e `--reinstall` (pip -e). Git/pip atrás de runners injetáveis (testável offline). `lohra/selfupdate/`. ⏳ casca Tauri depende de packaging. Tudo validado ao vivo. **Packaging (bundle macOS local)** ✅ — backend congelado (PyInstaller onedir) como **resource** do Tauri (`Contents/Resources/lohra-backend/lohra`); `backend.rs` resolve o sidecar (release: falha alto; dev: PATH). 4 gotchas do freeze em `docs/history/PACKAGING.md` (pathex, deps uvicorn[standard], onedir, **`lifespan="off"`**). **UI de settings da API key**: app do Finder não herda env do shell → backend lê `~/.lohra/.env` no startup (`lohra/config/`); painel de Settings grava via `config.rs` (`provider_status`/`save_provider_key`, chmod 600) + `restart_backend` + gate no boot. `.app` roda standalone, chat funciona end-to-end (validado ao vivo). `desktop/scripts/build-macos.sh`, `backend/packaging/`. ⏳ não-verificado (recursos externos): assinatura/notarização, appimage/msi, CI.

## Fase 7 — Orquestração de sessões paralelas (em andamento)
A Lohra-agente spawna sub-sessões paralelas, injeta steer e colhe async. Um core, dois consumidores (tool do agente + métodos WS). Spec em `docs/specs/06-orchestration.md`.
- **Milestone A** ✅ (validado ao vivo): `OrchestrationCore` (`lohra/orchestration/core.py`) — sub-sessões **independentes** (Agent isolado de subagente, `on_compaction=None`), `ThreadPoolExecutor` teto configurável; `spawn`/`steer`(busy→inbox, idle→novo turno)/`collect(wait)`/`cancel`/`shutdown`. Steer no loop (`run_conversation(inbox=…)` drena entre iterações → `<system-reminder>` no tail, Invariante #1 ok). Tool triad interceptada `spawn_session`/`steer_session`/`collect_session`, wirada CLI+gateway, excluída de subagentes/server. core 90% / tools 100%.
- **Milestone C** ✅ (validado ao vivo): `delegate_task` retomável — reusa o `OrchestrationCore` (não mais ThreadPool descartável); cada task vira sub-sessão persistida (`source="orchestration"`, filtrada do `list_sessions`), resultado carrega `sub_id`, e `resume_id` continua um filho existente (steer→collect). `DelegateTaskTool(core, parent_session_id)`; `build_session_dispatch` perdeu o param `child_factory`.
- **Limpezas** ✅ (3/3): inbox migrou pra `GatewaySession` (`enqueue_steer`/`drain_steers`; core perdeu o dict por sub_id) + eviction no `OrchestrationCore._children` (cap 200, só terminais) + parent linkage real no gateway (`AgentFactory` recebe `session_id`, fechado no M6 do CC-Parity).
- **Limites configuráveis** ✅: `resolve_limits` (em `orchestration/core.py`) — concorrência via `--max-parallel` (chat) ou `LOHRA_MAX_PARALLEL`; registry via `LOHRA_MAX_SUBSESSIONS`; flag > env > default (4 / 200). Inválido → default. Wirado no `lohra chat` e no dashboard (env-only).
- **Milestone B (métodos WS) ADIADO** — sem consumidor (o desktop não chama `prompt.background`/`session.steer`); o payoff agent-facing está em A+C. Requer restructure arriscado do WS. Ver `docs/ROADMAP.md`.

## Fase 8 — Harness de dynamic workflows — COMPLETA
A Lohra-agente **autora e roda dynamic workflows** (DAG declarativo typed, sem executar código autorado) com o rigor do Claude Code. Spec `docs/specs/07-workflow-harness.md`. Pacote `lohra/workflow/`.
- **A** ✅ — modelo + validador (`nodes`/`schema`/`refs`): node-types fechados; refs path-only single-pass (guard injeção 2ª ordem); `validate_spec` → WorkflowSpec|ValidationError didático (nunca levanta), e rejeita tipos válidos-mas-sem-strategy.
- **B** ✅ — `engine` (interpreter, ordem topológica, engine-fault isolado) + strategies `agent`/`parallel` + **leaf sandbox** (`sandbox.py`: fs path-allowlist deny-by-default + egress allowlist + taint; policy do operador, não do spec) + `budget` (fan-out/lifetime, nunca unbounded).
- **C** ✅ — structured output: `validation.py` (jsonschema) valida a saída do leaf; mismatch → steer de correção na mesma sub-sessão (bounded); falha → null.
- **E** ✅ — nós de rigor: **verify** (adversarial: N skeptics refutam, maioria mata), **judge_panel** (N attempts → judges → vencedor sintetizado), **loop_until_dry**.
- **F** ✅ — tool surface `run_workflow`/`workflow_status`/`workflow_cancel` (interceptada): `WorkflowService` valida → core sandboxed por run → roda em background → run_id na hora. Wirada CLI+dashboard, excluída de subagentes/server. Sandbox validado no caminho REAL (teste de integração + discriminador).
- **D** ✅ — pipeline **no-barrier**: core ganhou `spawn(on_done=…)` (hook não-bloqueante, 1x por sub-sessão, fora do lock, não no busy-handoff); `run_pipeline` encadeia cada item pelos stages independentemente (on_done não-bloqueante: collect wait=False + validate + re-spawn; barreira Event.wait no pool separado do service). Testes que definem correto: no-barrier + throughput (≤ pool_width, sem deadlock). Drop-on-invalid (v1).
- **G** ✅ — resume/cache content-addressed: tabela `workflow_node_cache` (key por conteúdo, run-scoped, cross-run OFF §6.3); get-or-spawn por célula (scalar + pipeline per-(item,stage)); **só completions são cacheadas** (dead/invalid re-spawnam no resume — LLM estocástico). `resume_run_id` re-roda reusando o cache; partial-resume só re-spawna o que faltou. budget lifetime guardado (sem cap silencioso).
- **J** ✅ — self-improvement: `rollup.py` (rollup rico, `null_rate` first-class) + `library.py` (run problemática → **prior** em `~/.lohra/workflows/insights.md` com lock+dedup+cap, **não** o MEMORY.md/prompt; run limpa → **template** validado). Tool `workflow_templates` (list+get, devolve {templates, insights}) + nudge no `run_workflow`. Run cancelada não grava prior.
- **H** ✅ — nesting (`workflow` node): recursão de engine determinística, `nested_engine()` compartilha core/budget/cache/loader (sandbox/budget não escapáveis), `MAX_WORKFLOW_DEPTH=1`; `fold_nested()` dobra métricas aninhadas no rollup do pai (J não certifica composto quebrado). **Todos os 7 node-types executam.**
- **taint real** ✅ — `agent/taint.py`: TaintTracker (sticky/sessão) + taint_wrap no dispatch; turno que ingere web/MCP → `run_workflow` roda leaves sem fs+egress. Discriminador no caminho real.
- **I — forced `tool_choice`** ✅ — `build_kwargs(tool_choice=…)` byte-idêntico no default; node `tool_less:true` + schema → `core.spawn(configure=…)` seta `agent.forced_tool` → loop força a synthetic tool (args=resposta), provider ignora → §5.1. Turno replay-safe. Fix do mix-up `schema:"nome"` vs `schema_ref` (resolve_schema coage + validador rejeita irresolvível).
- **Polish** ✅ — `aggregate_tokens` + `forcing_fallbacks` no rollup/priors (usage do leaf → core → RunResult, lock nas escritas off-thread do pipeline); retry-por-stage no pipeline (re-spawn fresco com correção, bounded). Tokens validados ao vivo.
- ~815 testes, ruff limpo. **Fase 8 completa (A–J + polish), tudo validado ao vivo.**

## Fase 9 — Consciência de projeto (instruções + skills do projeto) — COMPLETA
Rodando num projeto, a Lohra lê/segue AGENTS.md/CLAUDE.md e descobre/lê/edita/usa as skills do projeto. Implementada com **ultracode** (cada fatia gateada por review adversarial multi-agente antes do commit). `lohra/project/discover.py`, `lohra/safeio.py`.
- **A1** ✅ — `discover.py`: acha a raiz (VCS/agent root tem precedência sobre build-manifest — fix do dogfood `cd backend`), lê AGENTS.md/CLAUDE.md → `context_files` + `environment_hints` no prompt congelado. `safeio.read_text_bounded` (symlink/não-regular rejeitado, byte-bounded) — sem OOM/hang/exfil. Wirado CLI+dashboard.
- **A2** ✅ — `SkillStore(home, extra_roots)` escaneia `.claude/skills`/`.lohra/skills` do projeto (precedência sobre home), index rotula `(project)`; create/delete **home-only** (delete nunca apaga repo do usuário). `discover_skill_roots`.
- **A3** ✅ — `skill_manage`: `update` edita in-place onde a skill vive (projeto→projeto), `create scope=project`; `_under_roots` guarda escrita contra symlink-dir escape; OSError→tool_error; platforms round-trip.
- **A4** ✅ — "usar" emerge de A1+A2. E2E ao vivo: projeto com AGENTS.md (🦊) + skill pirata → Lohra usou a skill e seguiu o AGENTS.md. ~854 testes.

## Fase 10 — Auth por subscription (OpenAI/Codex SOMENTE) — ✅ COMPLETA, validada ao vivo (ToS-gray)
Opt-in, ToS-gray. Dois caminhos de login: **próprio** (`lohra auth login`, OAuth device flow + store próprio + auto-refresh) com precedência, e **reuse** do Codex CLI (`~/.codex/auth.json`, sem refresh). Construída com **ultracode** (design+pesquisa panel + review adversarial por fatia; constantes = fatos públicos citados ao repo `openai/codex`). `lohra/subscription/`. ~929 testes.
- **B1** ✅ credential subsystem: lê `~/.codex/auth.json` (safeio bounded/symlink-safe), store `~/.lohra/auth.json` (opt-in + `acknowledged_tos_risk` hard-gate fail-closed, chmod 600, sem token), expiry via JWT exp. Refresh em memória **não** reescreve o Codex (rotação→revogação); reprs redatados.
- **B2** ✅ Responses-API transport+client: o backend Codex só fala Responses. `store=false` + `stream=true` (exigidos, descobertos ao vivo); `ResponsesClient` sempre streama e remonta a Response dos `output_item.done` (o `completed` vem vazio sob store=false). Vision via `image_url→input_image`. `response.failed` levanta erro (não engole).
- **B3** ✅ wiring + `lohra auth` (status/enable/disable, aviso de ToS): run_chat **e dashboard** resolvem subscription ANTES do provider (alcançável sem API key); model default = o da config do Codex (`read_codex_model`, fallback gpt-5.5); **`lohra serve` RECUSA** (não vaza o token).
- **B4** ✅ robustez (token nunca no env; reuse do Codex não refresca — rotação corre com o Codex).
- **B5** ✅ (gap #1 FECHADO): first-party OAuth — `oauth.py` device flow (auth.openai.com, mesmo client_id, account_id via JWT claim, User-Agent obrigatório); `token_store.py` (`~/.lohra/oauth.json`, escrita atômica 0600, repr redatado); `resolve` refresca 5min antes de expirar + persiste o family rotacionado (SEGURO — token próprio; race→re-lê o vencedor). `lohra auth login`/`logout`.
- **Reasoning replay** ✅ (paridade opencode): `include=["reasoning.encrypted_content"]` + captura em `provider_data` + re-injeção no input (só items com encrypted state) → continuidade de raciocínio sob store=false.
- **Validado ao vivo** (subscription do usuário): chat · single/chained tools · usage/tokens · dashboard · **workflow fan-out** · **reasoning continuity** · **vision** · **login próprio + auto-refresh**. Review ultracode/advisor limpo em cada fatia.
- **Não-verificado** (nomeado): streaming WS no desktop sob subscription; fan-out grande (rate-limit — degrada gracioso via `response.failed`). `image_gen` falha gracioso sob subscription (Responses não faz Images API).

**Comparação com opencode** (prior-art): núcleo idêntico (Responses+store=false+bearer+mesmo client_id) E agora paridade no que faltava — reasoning replay + OAuth/store próprios com refresh transparente (B5).

## CC-Parity do workflow harness (2026-08-26/27) — COMPLETO, validado ao vivo
Campanha guiada pelo gabarito real dos dynamic workflows do Claude Code (scorecard 15 capacidades). Docs: `docs/history/ROADMAP-CC-PARITY.md` (M1–M7) · `docs/history/BACKLOG.md` (WF-1..30) · `docs/history/reviews/` (review 15-agentes + dogfoods). Suíte **959→1273**, 94% cobertura.
- **M1–M2 fail-closed**: falha NUNCA é silenciosa — fault com causa em todo caminho (container `${ref}`, upstream-null vira fault e não a string "null", verify fail-closed, status honesto `complete|degraded|failed|cancelled|paused`); done-path do pipeline blindado; liveness guard no resume.
- **M3+M3.5 autoria**: `SpecIssue.example` renderizado no erro; RUN_GUIDANCE com os node-types; **skill builtin `workflow-authoring`** (tier builtin no SkillStore: projeto > home > builtin, update = copy-on-write pro home) com testes de contrato anti-drift (fences validados de verdade).
- **M4 resiliência**: quota→`paused` (classificação por TIPO de exceção, nunca prosa) + auto-resume (backoff, cap 5, cold-start rearm); cancel real; cancel-on-timeout; `timeout`/`retries` por nó.
- **M5 token budget**: gate soft pré-spawn + gate de fan-out estimado nas barreiras; paused sem auto-resume (raise-only no resume, erro sugere valor); custo por célula (`workflow_node_cost`) + cumulativo (`workflow_run_spend`); `{total,spent,remaining}` até mid-run.
- **M6 operabilidade**: progresso mid-run por nó (engine vivo), `workflow_list`/`workflow_pause`, notificação via inbox de steer; AgentFactory recebe session_id (materializou o parent linkage adiado da Fase 7).
- **M7**: node-types 7→**10** (`gate` semântico, `completeness_check`, `checkpoint` humano journaled); tiers de operador (`~/.lohra/workflow_tiers.json`); contrato `NODE_SPECS ≡ STRATEGIES ≡ skill` testado; `fs_allow` com modo ro/rw.
- **WF-24..30 (dogfood via Codex)**: args reidratados no resume; cell-cache nos 5 node-types órfãos (resume re-pagava 93,6k → **0 tokens**, comprovado ao vivo); priors citam faults reais; `faults_total` cumulativo; **WF-29 estado durável** (`workflow_run_state` + lease com heartbeat + cold-start rearm — checkpoint funciona ENTRE processos/turnos do CLI, validado ao vivo com humano aprovando via o próprio checkpoint); WF-30 (race stop×tick do heartbeat, achada pela própria Lohra revisando o código do dia).
- **Pendências nomeadas**: WF-11 (worktree/leaf) adiado com racional; 3 candidatos do review da Lohra a triar (`docs/history/reviews/2026-08-26-dogfood-codex.md`); `builtin_root` sob PyInstaller não re-verificado. Skill `use-lohra` avisa o footgun de custo (profile novo não herda subscription).

## Standalone + PyPI + Live View (2026-08-26/27) — COMPLETO, validado ao vivo
O backend virou **runtime instalável e publicado**: `pip install lohra` (PyPI, 0.0.1→0.0.4).
- **Standalone**: wheel py3-none-any (Python 3.11–3.13; zero imports Unix-only; anthropic embutido nas deps base); LICENSE MIT; skill builtin no wheel; versão single-source (`importlib.metadata` — a fonte é o pyproject); `lohra update` fora de git ensina o remédio pip. `docs/STANDALONE.md`.
- **Kit de delegação**: `lohra/skills/export/use-lohra/` (package data, SEPARADO do builtin — skill p/ orquestradores não entra no índice da Lohra) + `lohra skill export <name> [--to DIR]`; anti-drift: cópia empacotada ≡ `docs/skills/use-lohra` (teste).
- **Validado ao vivo**: venv virgem do PyPI (macOS) e **Windows real** (`pip install lohra` + chat funcionaram — primeira execução fora do macOS). Codex headless bloqueado por política enterprise do usuário (interativo ok).
- **Live view** (`workflow/events.py`, `liveview.py`, `liveview_tui.py`, `watch.py`): o operador enxerga o run — DAG impresso na aceitação do spec, eventos node/items/fault/done ao vivo no stderr (faults NA HORA; custo escala item a item; 100% harness, zero LLM), modo TUI in-place (LiveBlock puro + LivePainter ANSI, `isatty`+`LOHRA_LIVEVIEW=fancy|plain|off`, sanitização de control chars — spec é autorado pelo agente), progress durável (`progress_json` — status/list cross-process de run terminada mostra nós reais), **`lohra workflow list|watch`** (espectador sem LLM, zero tokens; watch sai em terminal/sumida/stale). Validado ao vivo pelo usuário no terminal (2 rodadas) + PTY real.
- Suíte **1377**, 94% cobertura. Ideia anotada (pedido do usuário): "tempo real" — relógio por nó + tokens estimados `~` durante o voo dos leaves (estimativa NUNCA entra na contabilidade).

## Waves 4→7 (0.0.10→0.0.13) — auditoria, fencing, cancel/reserve, supervisão ativa
Pós-CC-Parity o trabalho passou de Fases para **Waves** rastreadas por GitHub milestones/issues, cada épico com aceite por avaliador independente. **Wave 4 — auditoria dos nodes** (0.0.10, épico OBS **implementado pela própria Lohra**, 5 issues): `lohra workflow audit <run_id>` + tool `workflow_audit`, ledger durável metadata-only com gaps honestos (tombstones/`$compacted`), `LOHRA_AUDIT` switch; spec `docs/specs/08`. **Fencing cross-process** (0.0.11, #12): `workflow_run_fence` monotônico por aquisição — toda escrita sob ownership apresenta a cerca; dono obsoleto recusado, nunca corrompe; + auth REST do gateway e rejeição de tool-call stream incompleto (PR #33). **Cancel/reserve** (0.0.12, #8/#14): cancel de leaf enfileirada dispara terminal na hora (status `cancelled`), `on_lease_lost` aborta a execução do dono expulso, `Budget.reserve()` atômico com refund só do que nunca rodou — fundação da Wave 6. **Wave 6 — supervisão ativa** (0.0.13, 6 issues SUP-01..06, **épico liderado pela Lohra**; quota 429 no meio → coautoria declarada da fatia final): doutrina vigiar→diagnosticar→adaptar→retomar com fronteira agente×humano e freios (1 contorno por `(run,causa,target)`, 3/run, K=2, allowance min(6k,25%)) + 37 contratos anti-drift; `workflow_status` com proveniência honesta; steering endereçado por identidade quíntupla (só ocorrência viva, lido entre iterações, prompt congelado provado por identidade de objeto); pivô = spec adaptado no mesmo `resume_run_id` reusando cache; aprendizado no erro (classificador fail-closed, notices duráveis claim/ack com dedup/cap/TTL, dead-turn notices inclusive nos descartes invisíveis, persistência de turno TRANSACIONAL `save_messages`); gate E2E CONFIRMADA (matriz cross-feature sem bug de produção + probes adversariais ao vivo — texto segura manchetes, não cláusulas sutis → #36). Módulos novos: `lohra/state/{notices,insights}.py`, `lohra/workflow/{failure_taxonomy,steering,supervision}.py`, `lohra/agent/notices_overlay.py`. **Wave 7 — robustez operacional (COMPLETA, 0.0.14→0.0.17, milestone fechado 4/4)**: #38 janela de contexto OpenRouter no preflight (fechada antes) · #34 audit sink — causa raiz era MIGRAÇÃO faltante (tombstones.next_seq) mascarada de contenção; migração + busy_timeout 250ms do operador (LOHRA_AUDIT_BUSY_TIMEOUT_MS) + warnings agregados por tempo + drain final de markers (0.0.16) · #39 rastro de acks — tombstones bounded `notice_trail` (atômicos com o DELETE; acked/expired/evicted com token; cap 64 + TTL 30d + varredura global) + `lohra notices <session_id>`; contrato claim/ack intocado; tombstone-sobre-flag registrado em docs/history/2026-08-31 · #40 morte por sinal — `agent/signals.py`: SIGTERM/SIGHUP viram TerminatedBySignal e o epílogo NORMAL publica dead-turn notice + envelope --json + morte fiel 128+N (die_by_signal); desarme-no-1º-sinal, SIG_IGN/nohup respeitado, defer_signals nas janelas de persistência, canonical_done antes do ack (nunca fato falso); SIGINT sob --json com a mesma semântica; review adversarial (ultracode) achou e matou o join do pool de tools segurando o unwinding (0.0.17, validado ao vivo com kill real). Fillers fechados: #24 (watch/audit por prefixo curto), #35 (--provider sobrepõe subscription sob auto). Abertos: #37, #41; #36 gateado por decisão de produto. Também na 0.0.15: custo em USD no envelope --json (`lohra/pricing/`: fail-closed, api_equivalent para subscription, usage_total somando o turno inteiro) + skill use-lohra com cost/override/notices (anti-drift cobre a cópia .codex).

**Wave 7.5 — fatias paralelas verificadas-no-código (2026-09-01, 0.0.18, branch `integration/wave-7.5`, 5 worktrees em `../lohra-wt`, agentes opus/sonnet coordenados + review adversarial nas fatias de risco)**: #4 leaf sandbox nega `terminal`+`mcp_*` por default (exfil via shell/MCP estava aberta até em run tainted; opt-in SÓ do operador via `workflow_policy.json`/`LOHRA_LEAF_ALLOW_TERMINAL`/`LOHRA_LEAF_MCP_ALLOW`; definitions filtradas) · #49 lint de DAG desconexo (`warnings` no aceite) + #2 `depends_on` malformado rejeitado (era descartado em silêncio e reordenava) · #47 só observabilidade (não era deadlock: notice para sessão sem próximo turno + `watch` em loop infinito em `paused`) — `watch` sai em pausa sem auto-resume, `pause_reason` em list/watch, campo `workflows` no envelope `--json`; teto de operador PENDENTE (decisão do dono) · #48 reescrita (leaves streamam; `LEAF_TIMEOUT=120s` morde antes dos 600s HTTP; retry M4 não re-executa leaf morto) — `LOHRA_PROVIDER_READ_TIMEOUT` (default byte-idêntico) + `error_kind="timeout"` + fault didático · #8/#42-B quiescência após cancel (`workflow/quiescence.py`, teto 5s `LOHRA_CANCEL_QUIESCENCE_S`, fault diz settled × STILL RUNNING) + **#15 decidido: `required: true` real** (`workflow/required.py`; abort → `failed`, restantes `skipped` não-nulados, fora do cell_hash, nested sobe; `min_success_ratio` segue ignorado — spec ambíguo) + triagem dogfood-codex (i refutado; ii corrigido: cancel não reescreve veredito terminal; iii confirmado, xfail estrito). **Validado ao vivo via Codex headless** (2026-09-02, `docs/history/reviews/2026-09-02-dogfood-codex-wave7.5.md`): 6 testes, todos com o comportamento esperado; T1 divergiu só na expectativa (leaf não VÊ `terminal` — filtro de definitions — em vez de receber a negação didática). Pendências nomeadas dos reviews (web_search fora de `egress_allow`, redirect×allowlist, taint_wrap no leaf, N×teto em fan-out, null_rate com skipped, auto-resume precoce) em `docs/history/2026-09-01-wave7.5-parallel-slices.md`.

**Wave 8 prelúdio (2026-09-02, 0.0.19, integração LINEAR sem merge commits — o `release.sh` espelha por cherry-pick)**: decisões do dono que faltavam viraram código — **#15 fechada**: `min_success_ratio` removido do schema com erro didático `min_success_ratio_removed` (nunca `unknown_field`) apontando `gate`/`completeness_check` marcado `required` · **#47 fechada (parte 2)**: teto de tokens pré-autorizado pelo OPERADOR (`workflow/operator_budget.py`: `--token-budget-cap` > `LOHRA_TOKEN_BUDGET_CAP` > sem teto; efetivo = min(spec, cap), cap sozinho quando o agente não pede; resume clampado; proveniência `token_budget: {total, source, operator_cap}` no aceite; hints apontam o operador; teto é POR RUN — teto process-total é pendência nomeada; review adversarial: sem bypass, 4 gates textuais/proveniência aplicados) · #36 segue gateado. **Wave 8 aberta como investigação read-only** sobre a run real `42abc3eb…` (banco do profile lohra-notion-v4), 3 relatórios em `docs/history/reviews/2026-09-02-wave8-investigation.md` e comentários em #42 #43 #44 #45: cache CORRETO (0 reexecuções incorretas; falta explicabilidade — preview de blast radius pré-resume é o épico S de maior valor); zumbi do cancel agiu 156 s após o sucessor (causa: `loop.py:426` despacha tool calls sem checar interrupt — épico S), dano não observado; 3/5 artefatos de células mutados por leaf vivo, prejuízo zero (spec sem `${ref}`); pivô do v4 foi HUMANO verbatim (SUP-01 funcionou), custo real = 55% dos tokens fora de célula por degradar em silêncio — opções A/B/C para o dono (recomendação: pausar `route_fault` antes de envelope de rotas). 10 épicos ordenados na tabela do relatório. `workflow_insight_candidates` vazia no run = evidência para a Wave 9.

**Wave 8.1 (2026-09-03, 0.0.20, integração linear)** — os 5 épicos S da tabela da investigação, todos entregues: E1 interrupt checado antes do dispatch (`loop.py`; review adversarial: correto, 4 gates de docstring; residual: flag por LOTE) · E2+E3 `cache_preview` no aceite de resume + `reason` no `cache.missed` + identidade da spec no `segment.started` (stand-in engine roda as strategies reais e para no lookup; pipeline/workflow = `unknown`; D6 tratado com peek por prefixo de fan-out) · E4 re-spawn same-route em falha terminal, OPT-IN por `retries` explícito (denylist quota/timeout/budget/admin/auth_failed; só `agent`; review adversarial achou e matou o shadowing de `attempt` → TypeError) · E5 texto honesto da quiescência + contrato `work-{fence}`. Dogfood T8 via Codex PASS (modelo inexistente no openrouter, `retries: 2` → exatamente 3 `leaf.started`, faults `attempt 1/3..3/3` + esgotamento, status `failed`, 0 `cache.stored`; controle `retries: 0` → 1): `docs/history/evidence/wave8.1-dogfood/`. **Pendências nomeadas**: `auth_failed` estrutural entrou (denylist), mas não é razão de pausa; re-spawn por falha terminal é opt-in (`retries` explícito) — decisão do coordenador com base no run v4 (os 8 nós declaravam `retries`); Q2 pendente: descontar fault de tentativa recuperada em `derive_status` E `carried_faults` com contador `leaf_respawns` (fatia própria — hoje o run que recupera na 2ª tentativa sela `degraded`); preview não cobre `pipeline`/`workflow` (v2); o turno do `lohra chat --json` não é interrompível hoje (só o gateway chama `request_interrupt`) e o envelope não tem sinal de interrupção — pré-existente.

**Wave 8.2 (2026-09-03, 0.0.21, integração linear)** — Q2 `leaf_respawns` (desconto por identidade/multiset, por estiramento; `prior_recovered` durável; template carimbado) · #43-C pausa `route_fault` (gatilho estreito auth_failed | série declarada esgotada; hint SUP-04; faults da série descontados como pause-caused; review adversarial: ver gates) · #45-E4/E5 manifesto de artefato (escopo `runs/<run_id>/` + `fs_allow`, realpath re-check; colunas laterais; `artifact_changed` no replay e no `cache_preview`; skill 799/800 após cortes de prosa) · #42-E3 interrupt aborta stream (`usage_uncertain` nunca 0; `_cancel_inflight` de quota agora para streams; review adversarial: ver gates). **Decisões assumidas** (dono pode vetar): gatilho da pausa; run recuperado certifica template; mismatch de manifesto = re-spawn; harness só mede dentro do escopo do sandbox; alegação divergente do leaf → fault real (`degraded`); uso de stream abortado = incerto. Dogfood T9 via Codex PASS (modelo inexistente + `retries: 1` → `paused/route_fault` com payload `{node_id, provider, model, last_error}`; envelope `workflows` com `pause_reason: route_fault`; `watch` saiu em <1 s com o hint SUP-04; resume adaptado no MESMO run → `cache_preview {replay: 1, never_completed: 1}` → `complete`, `ok` replayado com 1 só `leaf.started`): `docs/history/evidence/wave8.2-dogfood/`. **Pendências nomeadas**: #43-B envelope de rotas (depende de #46 USD por rota e contador durável #36); freio de resume repetido na mesma rota morta; `auth_failed` como pausa em stages de pipeline (paridade); caminho não-streaming e tool em voo não abortáveis; a latência do abort é o intervalo até o próximo evento (modelo de raciocínio sob Codex sem `reasoning.summary` fica sem evento durante o raciocínio — issue #59); corrida pré-existente em `account_leaf` (lê `collect(wait=False)` uma vez e deduplica: leaf ainda `running` grava 0/False como fato — alcançável via `_timed_out` quando a quiescência expira); preview v2 para `pipeline`/`workflow`; `route_fault` sem freio para resume repetido na mesma rota morta (candidato ao E6 da #43; cada resume manual também incrementa o `attempts` compartilhado com o auto-resume de quota).

**Wave 8.3 (2026-09-03, 0.0.22, integração linear)** — #59 `reasoning.summary` (medição ao vivo: espera de cancel na fase de raciocínio 4,5 s → 2,3 s; premissa do silêncio parcialmente refutada; switch default `auto`) · corrida de `account_leaf` (terminal-only + `core.watch_done` + late usage incerto no seal com duas causas distintas — voando × evicta; review adversarial: corridas refutadas em 300 iterações, gates de docstring/causa aplicados; follow-up #60: status de sub-sessão steerada não volta a `running`) · fix do teste instável do `parallel` route_fault · **Wave 9 T0 (#54) investigada read-only sobre os stores reais** (`docs/history/reviews/2026-09-03-wave9-t0-taxonomy.md`): 700 sessões, 29 roots → 1 memória (fato ambiental salvo CORRETAMENTE pela guidance vigente), **1** candidate (não 0), 16/16 priors legados boilerplate com conselho na direção errada em 15; **0/57 notices com causa atribuível a agência** (sustenta a nula da #52); fingerprint de candidates hasheia texto livre e não conta recorrência (condição de falsificação da #50 já verdadeira); `recent_insights` descarta mecanismo/confiança (#51 confirmada por código); #53 indecidível sem experimento. Épicos propostos E1–E7 (todos S salvo E5) e lista "nunca memorizar". **Pergunta ao dono**: modelo inexistente é `environment` ou `agency`? (7/57 notices + 10/16 priors; muda a #50). **Pendências nomeadas**: leaf sem `effort` não ganha janela de abort; `reasoning_callback` sem chamador em produção (live view não mostra raciocínio); `detailed`/`concise` não exercitados; #43-B ainda depende de #46/#36.

**Wave 8.4 (2026-09-03, 0.0.23)** — decisões do dono aplicadas: (1) `route_fault` respondida por comando via `checkpoint_answers` (canal reusado; 9 recusas em ordem; `abort` = `cancelled`; nested recusado na v1; review adversarial: ver gates) · (5) alegação divergente de manifesto = fault advisory não-degradante (`advisory_faults` com as 3 pernas do padrão Q2; `meta.artifact_divergences` no template) · decisões 2–4 (retries opt-in; run recuperado certifica; mismatch = re-spawn) mantidas pelo dono. Dogfood T10 via Codex: mecanismo PASS de ponta a ponta (pausa → hint com a forma do comando → resposta para nó errado recusada, run segue pausado → resposta certa aceita sem spec, `rerouted {from,to}` ecoado, `cache_preview {replay: 1}`, `ok` replayado com 1 `leaf.started`, `complete`, `route`/`reason` somem do status); única expectativa não atendida foi minha: o ledger de audit não tem evento tipado de re-rota (a prosa vive em `faults_total`) → #64 `node.rerouted`. Achado colateral corrigido: a skill use-lohra documentava `lohra workflow list --profile`, que o CLI rejeita (é `LOHRA_PROFILE=`). Evidência: `docs/history/evidence/wave8.4-dogfood/`. Correção de rota AUTOMÁTICA (a Lohra contornar sozinha) fica para #63 (envelope pré-autorizado; depende de #46 e #36). **Wave 8**: tudo o que era decidível está entregue; restam #42 (residual do abort), #43 (só a opção B → #63), #60, #61, #62, #63.

**Wave 8.5 (2026-09-03, 0.0.24) — milestone 10 FECHADO.** #64 `node.rerouted` · #61 preview v2 + replay visível · #60 status steerado (review: cunhagem de lifetime achada e fechada, `landed`) · #63 envelope de rotas (review adversarial: ver gates) · #62 experimento (A invisível 24/25; manifesto por sorte; recheck duplica; C2 aceito-e-ignorado → #65/#66/#67) · #42 fechada com veredito (parte A: não implementar) · #43 fechada com o envelope. Residual do abort de tool em voo → #68 (L, fora do milestone). Frestas pré-existentes do steer → #69; flake de ordem dos testes de sinal → #70. Dogfood T11 (direto; o Codex morreu no teto de 10 min após a 1ª tentativa ser recusada por preço desconhecido — fail-closed correto): com override de preço no profile e envelope presente → `complete` sem pausa, `node.rerouted` no audit, `leaf_respawns: 2`; envelope esgotado → `paused/route_fault` com `route.envelope`. `docs/history/evidence/wave8.5-dogfood/`. **Decisões do coordenador nesta rodada**: um julgamento por morte no envelope; subscription nunca candidata por preço; nó `agent` sem routing declarado é elegível ao envelope (bound por rota morta segura os irmãos) — assimetria com a resposta por comando (que recusa rigor sem routing) nomeada; dropado-após-faturar = `interrupted`. **Wave 9 aguarda a decisão do dono (environment × agency).**

## Orquestração — extensões (na main, validadas/revisadas)
- **`lohra chat --json`** ✅ — envelope estruturado completo p/ orquestração (input/output/reasoning/tool_calls com result/model/usage/stop_reason/error). stdout = SEMPRE 1 JSON parseável (até em erro: error_envelope; `ensure_ascii=True`; streaming off). `lohra/agent/result_json.py`. Validado ao vivo. Skill pronta: `docs/skills/use-lohra/`.
- **Multi-model + reasoning effort por subagente** ✅ (same-provider) — `agent/overrides.py make_configure` via o hook `core.spawn(configure)`; campos `model`/`effort`/`provider` no workflow node + `delegate_task` + `spawn_session`; `Agent.effort` + `build_kwargs(effort=…)` (responses→`reasoning:{effort}`, chat→`reasoning_effort`, anthropic no-op). Effort validado ao vivo no Codex. Fechou o footgun do campo declarado-mas-ignorado.
- **Cross-provider delegação** ✅ — `agent/client_pool.py ClientPool`: orquestrador no provider X, subagente/leaf em provider DIFERENTE (Claude→GPT→Codex). Build+cache por provider (lock), borrow do client do pai (fecha só os próprios), gate de escalação de subscription (`openai-codex` só via `subscription_active`), provider-error fail-isolado, swap atômico provider+client. Cross-provider ao vivo precisa de 2ª key do usuário; same-provider + subscription validados. ~959 testes.


**Wave 10 (2026-09-05, 0.0.25) — milestone 12 "Contratos do harness".** Nasceu da revisão de 17 padrões de DAG contra o código (o run TS de 129k/18k tokens era gate soft por desenho — e rodava SEM store no lohra-ts). Método por épico: hipótese → teste RED do experimento → veredito na issue → intervenção → review adversarial → fix round → re-review → rebase linear. Entregues: #71 (teto = linha de parada com média medida; `overrun`/`overrun_max`; medição semeada por leaves), #72 (guard de buraco de agregação, 3 portas, pipeline distingue morte de null), #73 (`label`/`phase` removidos, `pool_width` removido, `loop_until_dry.budget` real, anti-drift estrutural), #74 (checkpoint `accept`/`on_reject`, sem `default` em gate guardado, completeness `required`), #75 (carimbo de política/versão, advisory agregada, contadores por fonte), #76 (pino de faults mistos). Reviews adversariais pegaram: média semeada 4,5× inflada por contar células (#71), `budget_overrun` zerado no caminho canônico (#71), fórmula de `artifact_divergences` incompatível entre fatias (#75), `default` ressuscitando um "não" por duas portas (#74), null legítimo de schema confundido com morte (#72). Nascidas na rodada: #77 `parallel.retries`, #78 colisão de id de checkpoint pai×aninhado (high), #79 template aninhado inválido sem fault, #80 import preguiçoso dos SDKs (~1 s a frio; suspeito de #70), #81 `list`/`watch` sem `overrun`. Dogfood T12/T13: `docs/history/evidence/wave10-dogfood/`. **Wave 9 segue gateada (environment × agency).** Próximo: fechar #77-#81 (S) ou decidir Wave 9.

**Wave 10.1 (2026-09-05, 0.0.26) — milestone 12 quase fechado (restam #87 #89 #90, nascidas nesta rodada).** Nove issues S (#65 #66 #67 #77 #78 #79 #80 #81 #82), todas com RED do experimento antes da intervenção; review adversarial em #65 #77 #78 #80 #82 (todas MERGE WITH FIXES → fix round → re-review MERGE). O que os reviews pegaram: `is_shared` como isenção permanente de `artifact_changed` (#65 → discriminador por sha); chave de checkpoint aninhado por TEMPLATE ainda abria dois portões (#78 → chave pelo id do nó `workflow`); gate de budget no re-spawn escapando da barreira e leaves em voo sem cobrança (#77); denominador de #71 diluído por tentativas mortas (#77); refusal de `id`/`type` quebraria 3 templates da library (#82 → aviso). Hipótese refutada: #80 NÃO explica #70 (classificação inalcançável ali). Nascidas: #87 (schema string/`schema_ref` em synthesize/body). Dogfood T14-T16 (#78 PASS ao vivo; T15/T16 probes inválidos por desenho): `docs/history/evidence/wave10.1-dogfood/` → #89 (recusa de sandbox invisível), #90 (células aninhadas por template colidem). **Wave 9 em curso em paralelo** (`integration/wave9`): E1 E2 E3 E4 E6 integradas; E8 (#85, decisão do dono) em review; E7a (#88) implementando; #52 fechada com a nula mantida; #86 (E5) adiada por medição (2 insights em 32 bancos); #50 parte 2 = gatilhos (após E8).

**Wave 9 (2026-09-05, 0.0.27) — milestone 11 "Feedback loops operacionais".** Gate aberto pela decisão do dono em #54 (`agency`; por instrução humana, substituir por modelo existente adequado e avisar). Entregues: E1 fingerprint estrutural + `hits` (#50) · E1b avisos de lint como candidatos (#50; censo decidiu NÃO cablear checkpoint rejeitado, required/completeness, route_fault corrigido, buraco de agregação — atribuição indeterminada) · E2 evidência estruturada no consumo (#51) · E3 guidance de memória e skills com a taxonomia (#83) · E4 proveniência de template (#51) · E6 anti-drift de escopo dos subagentes (#84) · E7a envelope do `delegate_task` (#88) · E8 substituição por catálogo (#85; review adversarial pegou a pausa `route_fault` tornada inalcançável e a âncora `small` não ratificada; dogfood T17 pegou o corpo real do OpenRouter sem código estrutural → tabela fechada de fingerprints capturados, exceção documentada à regra "nunca prosa"). Fechadas com veredito: #52 (nula mantida: 6/57 notices viram agency, todas eliminadas por E8; 1/57 consumida); #86 (E5) adiada por medição (2 insights em 32 bancos; só AGENCY é aprendível; o problema era o funil do produtor). #53 (E7) inventário estático: `fold_nested` preserva quase tudo; perdas reais → E7a e E1b. Fato para o futuro: toda a frequência observada dos gatilhos de agência está em um profile de QA; o orgânico tem zero. Dogfood: `docs/history/evidence/wave9-dogfood/`. Restam no milestone 12: #87 #89 #90.

**Wave 10.2 (2026-09-13, objetivo #100; sem release PyPI)** — conclui as três pendências do milestone 12: #87 permite schemas nomeados em `judge_panel.synthesize` e `loop_until_dry.body`, preservando hashes legados inline/ausentes; #90 separa cache e aprovações por chamada aninhada, com compatibilidade conservadora e custos atribuídos por `node_path`; #89 registra recusas reais de sandbox como advisory por nó e contagem de eventos retidos no audit, sem alterar a política nem degradar status por uma recusa. #101 corrige o timeout do SDK Anthropic que já quebrava a CI da base. PRs: [#103](https://github.com/marcelusfernandes/lohra/pull/103), [#104](https://github.com/marcelusfernandes/lohra/pull/104), [#105](https://github.com/marcelusfernandes/lohra/pull/105), [#107](https://github.com/marcelusfernandes/lohra/pull/107).

As hipóteses foram testadas antes da intervenção. T14 não comprovava reexecução indevida de um leaf completo; sua interpretação foi corrigida mantendo a evidência raw. A revisão independente encontrou uma colisão adicional de custos entre ids de raiz e labels aninhados, corrigida com reservas de ids e testes das duas ordens. No #89, o cutoff é a última snapshot por leaf incorporada antes do seal: o audit pode conter eventos posteriores que não entram naquele advisory; o discriminador comprova advisory=1/audit=2, sem reabrir o rollup. Aprovações legadas sem autoria comprovada exigem nova resposta; nenhum banco real foi migrado para validar isso.

Validação conjunta com clientes roteirizados e SQLite sintético: Python 3.11, 3717 testes mais 45 focados após o follow-up de comentário/teste do cutoff; Python 3.13, 3718 testes no head final; 95% de cobertura, ruff e diff-check limpos. Os quatro probes cruzando schemas nomeados com duas chamadas aninhadas passam, com replay sem novos leaves e invalidação após mudar o schema. Não houve provider real nesta rodada; os dogfoods históricos continuam identificados pelas suas próprias datas. Relatórios: [schemas](history/reviews/2026-09-13-wave10-schemas.md), [identidade aninhada](history/reviews/2026-09-13-wave10-nested-identity.md), [recusas de sandbox](history/reviews/2026-09-13-wave10-sandbox-denials.md). Evidência de CI e merges fica no [objetivo #100](https://github.com/marcelusfernandes/lohra/issues/100).

O dono definiu em [#102](https://github.com/marcelusfernandes/lohra/issues/102) que a independência da revisão vem de um agente isolado e agnóstico, não de outra identidade GitHub. Os vereditos são publicados por `marcelusfernandes`; o [ruleset](https://github.com/marcelusfernandes/lohra/rules/23216701) exige os dois checks Python e `review/isolated-agent` por commit. Novos heads passam novamente pela revisão registrada e pela CI; a integração usa squash sem bypass. A configuração não concede acesso a outra conta e não altera o plugin instalado.

**Continuação autônoma (2026-09-13/14, objetivo #108; sem release PyPI).** Entregues em PRs próprias:

- [#106 / PR #109](https://github.com/marcelusfernandes/lohra/pull/109): respostas estruturadas por endereço de checkpoint, com compatibilidade conservadora para mapas legados; a ambiguidade textual não é mais pendência. [Relatório](history/reviews/2026-09-13-checkpoint-addresses.md).
- [#55 / PR #110](https://github.com/marcelusfernandes/lohra/pull/110): `web_search` em leaves exige opt-in explícito do operador; taint continua negando. Policy efetiva e replay carregam a mudança. [Relatório](history/reviews/2026-09-13-workflow-search-opt-in.md).
- [#56 / PR #113](https://github.com/marcelusfernandes/lohra/pull/113): cada redirect de fetch respeita os hosts autorizados antes de DNS. IDNs usam a identidade HTTPX/IDNA e o fingerprint registra a nova semântica. O revisor encontrou e exigiu a correção de uma falsa recusa de redirect relativo Unicode antes do merge. [Relatório](history/reviews/2026-09-13-workflow-fetch-redirects.md).
- [#97](https://github.com/marcelusfernandes/lohra/issues/97): acessos de arquivo que compartilham o mesmo arquivo conhecido (inclusive aliases existentes) dentro de uma mensagem seguem a ordem emitida; recursos distintos continuam concorrentes. Isso não é guarda de staleness nem coordenação com writers externos.
- [#9](https://github.com/marcelusfernandes/lohra/issues/9): clients de assinatura vinculam o profile e a origem do login, revalidam gates e capturam token/account juntos a cada requisição. Refresh próprio, login/logout e configuração compartilham um lock nativo por profile; a família renovada é persistida antes do uso. O Codex é apenas relido. Falhas locais de auth são tipadas e não provocam retry de workflow ou fallback pago; o SDK não repete requests dinâmicos automaticamente. Streams abertos mantêm seu snapshot; crash entre rotação remota e persistência local pode exigir novo login. Testes usam stores, relógios e transports sintéticos; não houve autenticação real nem validação nativa no Windows nesta fatia.
- [#13](https://github.com/marcelusfernandes/lohra/issues/13): o transporte próprio de fetch valida a resolução usada por cada conexão e disca somente IPs públicos desse snapshot. Mantém Host/SNI/certificado por origem, fallback numérico sob um único prazo TCP/TLS e recusa proxy efetivo com erro de configuração. CAs de ambiente continuam verificadas; DNS bloqueante do SO fica fora do prazo de conexão. Clientes Python injetados são uma costura trusted e não recebem garantia de pinning. O fingerprint marca a nova semântica sem invalidar caches anteriores. [Contrato](specs/10-web-fetch.md).
- [#129](https://github.com/marcelusfernandes/lohra/issues/129): manager de aprovação por dispatch vivo, com binding interno à execução de cada chamada. Cache, callback e yolo não atravessam invocações ou consumidores; compaction do mesmo Agent mantém a continuidade, enquanto revival/nova invocação cria contexto novo. Gateway sem callback próprio e guards de filhos/serve/workflow permanecem deny. O objeto legado é importável, mas precisa ser passado explicitamente pelo embedder para ter efeito. Não há grants duráveis nem nova UI de aprovação.
- [#69](https://github.com/marcelusfernandes/lohra/issues/69): busca, aceite e publicação de steer compartilham a decisão sob lock; um turno já encerrado ou submit recusado devolve erro sem deixar instrução órfã nem alterar o estado antigo. Cada submissão recebe autorização própria somente após publicação bem-sucedida: uma chamada enfileirada antes da falha de criação de thread não executa, mesmo após outro steer aceito. A inbox é capturada sob locks curtos e seus callbacks executam depois, inclusive a liberação SQLite de supervisão. Cancelamento durante esses callbacks impede uma nova continuação; medidores já recebidos permanecem. Isso não interrompe à força ferramentas ou requests em andamento (#119).

- [#126](https://github.com/marcelusfernandes/lohra/issues/126): cancelamento e conclusão disputam a mesma transição durável. A conclusão vencedora recusa cancel tardio; cancelamento vencedor não vira failed/complete por uma cauda atrasada. O caminho ownerless preserva metadados atuais e verifica a lease na mesma transação. Snapshots antigos não restauram estados anteriores, e retomada automática exige a pausa autorizada na aquisição. Persistência recusada não responde sucesso; replay explícito permanece disponível. Publicação, eventos e limpeza de lease conservam a identidade da execução, sem alterar o fence financeiro original. A decisão funcional pode aparecer no status/watch antes do fechamento da contabilização: #111/#112, identidade de timer (#127) e criação de sub-sessão recusada (#136) continuam separadas. [Relatório](history/reviews/2026-09-14-workflow-cancel-transition.md).

- [#136](https://github.com/marcelusfernandes/lohra/issues/136): a criação de sub-sessão publica Future e identidade antes de autorizar sua própria execução. Se o executor enfileira e depois recusa a submissão, o callable recusado não produz efeitos, mesmo após outra tentativa aceita. Recusa preserva filhas anteriores, inclusive quando o registro atingiu sua capacidade; a preparação nova é encerrada como `spawn_rejected`. Os dois funis de leaf do workflow conservam a devolução única de lifetime, sem execução oculta. A submissão do workflow no nível Service (#138) permanece separada.

- [#138](https://github.com/marcelusfernandes/lohra/issues/138): o serviço publica Future/identidade antes de autorizar a execução daquela submissão. Recusa após enqueue permanece sem efeitos mesmo que o callable drene antes da limpeza; falhas de preparação liberam somente recursos próprios e preservam o erro original. A restauração do estado anterior e do cap configurado usa uma escrita condicional da aquisição atual, sem alterar medidores de uso nem apagar cancelamento ou sucessão. Uma preparação nova recusada registra `launch_failure`, sem inventar resultado de leaf; uma falha do próprio storage pode impedir a restauração e fica explícita no diagnóstico. PLAN/recovery/segmento são emitidos apenas pelo preâmbulo aceito, antes da primeira leaf; `started` já pode retornar antes de o DAG aparecer. Preparação, callbacks e cleanup ficam fora do mutex de admissão, e shutdown espera as preparações liberando esse mutex antes de drenar produtores e fechar os sinks. Não há retry automático para ocultar trabalho recusado.

A #9 passou 393 testes focados e 15 de onboarding em cada Python 3.11/3.13, além de 25 testes por runtime com SDK 1.66.0 isolado. Os testes incluem processos realmente concorrentes e recusa de auth no caminho WorkflowService → Agent, com uma leaf e nenhuma chamada de modelo apesar de `retries: 3`. [Relatório](history/reviews/2026-09-14-subscription-requests.md) e [contrato](specs/09-subscription-auth.md).

A #13 passou 437 testes focados em cada uma das quatro combinações Python 3.11/3.13 × HTTPX 0.27.0/0.28.1, com HTTPCore 1.0.9. Incluem conexão instrumentada no fetch padrão, TLS local com CA temporária, política de proxy comparada ao cliente HTTPX e replay SQLite sem novas folhas. [Relatório](history/reviews/2026-09-14-public-web-pinning.md).

A #129 passou 470 testes focados em cada Python 3.11/3.13; a cobertura dirigida de approval foi 100%. CLI, GatewaySession, compactação/retomada e dispatch reais foram exercitados com clientes roteirizados e executor fake, sob uma guarda que proíbe shell real. [Relatório](history/reviews/2026-09-14-session-approval-scope.md).

A #69 passou 430 testes focados em cada Python 3.11/3.13. Os testes incluem executor realmente fechado, falha de criação de thread após enqueue, retry aceito sem reviver a chamada recusada, eviction, callbacks reentrantes, cancel/shutdown durante settlement e WorkflowService/Core/SQLite temporários no caminho de supervisão. Cobertura dirigida: core 89%, GatewaySession 90%, agregado 89%. [Relatório](history/reviews/2026-09-14-steer-lifecycle.md).

A matriz final da #126 passou 458 testes focados em cada Python 3.11/3.13, com o strict xfail da #127 preservado. O guard de publicação teve 88% de cobertura dirigida com branches. Os discriminadores incluem processos concorrentes, recusa de persistência, troca de aquisição no mesmo store, publicação atrasada após sucessão, morte do processo e leitura por outra conexão durante o fechamento. São clientes sintéticos, sem provider ou banco pessoal; o caminho nativo de Windows foi apenas simulado.

Na publicação final da #126, aquisição e efeitos do mesmo run são serializados entre processos. Uma aquisição concorrente responde ocupada; um callback preso nessa etapa impede a sucessão daquele run até retornar ou o processo morrer, mesmo após o TTL. A recuperação por TTL continua para a execução fora dessa janela. O guard específico não envolve o drain financeiro nem mantém mutexes da Service/Core/RunState/Store ou transações SQLite durante callbacks.

A #136 passou 421 testes distintos em cada Python 3.11/3.13: matriz principal de 349 e complemento de 97, com 25 casos em comum. Os testes exercitam executors reais, recusa após enqueue, BaseException, tentativas posteriores, eviction, limpeza de preparação e callbacks rápidos/reentrantes. Os controles adicionais preservam accounting e settlement; a preparação teve 100% de cobertura dirigida de statements/branches. Clientes e tools são sintéticos, sem provider ou estado pessoal. [Relatório](history/reviews/2026-09-14-spawn-submission.md).

A #138 validou inicialmente 568 testes distintos em cada Python 3.11/3.13, com 1 xfail da #127 preservado. A matriz inicial teve 567 passes e uma falha de assinatura em um wrapper de teste antigo; corrigido apenas o encaminhamento dos novos argumentos, os 12 testes daquele arquivo passaram nos dois runtimes. A revisão independente encontrou ainda uma lease presa quando a instalação de seu timer falhava antes de retornar a aquisição. O reparo revoga a renovação e limpa o fence exato, preservando sucessores; adicionou 24 testes aos 28 iniciais. Seu foco final passou 244 testes e 1 xfail por runtime, incluindo os caminhos de heartbeat, fencing e Service. Essas contagens sobrepostas não são somadas. [Relatório](history/reviews/2026-09-14-workflow-submission.md).

CI das PRs #109/#110/#113: respectivamente 3753/3812/3896 testes passaram em cada Python 3.11/3.13, com 1 skipped, 1 xfailed e 95% de cobertura. A #97 passou 183 testes focados em cada runtime, incluindo aliases por identidade do filesystem e SIGINT/SIGTERM em subprocessos; sua PR #123 passou 3934 testes, com 4 skipped, 1 xfailed e 95% de cobertura em cada runtime. [Relatório](history/reviews/2026-09-13-file-call-order.md). As matrizes completas dos SHAs finais ficam nas PRs e no histórico das issues. Revisões independentes e os três checks obrigatórios precedem cada merge squash, sem bypass; a main local é reconciliada por árvore equivalente à pública, preservando as histórias distintas.

- [#127](https://github.com/marcelusfernandes/lohra/issues/127): o prazo do auto-resume é persistido com a pausa autoritativa, e o timer só recebe permissão após o Future anterior terminar. O plano verifica a identidade funcional da pausa e o fence na aquisição SQLite, sem confundir progresso com uma pausa nova. Cancelamento e aquisição vencedora invalidam o plano anterior; falha antes de adquirir preserva um plano ainda válido. Recuperação conserva o prazo original, deduplica planos e só conta novos timers aceitos; uma lease estrangeira viva é ignorada, sem varredura periódica esperando sua expiração. Após uma submissão recusada e a pausa restaurada sob novo fence, o timer antigo continua inválido; uma nova recuperação explícita pode reconstruir a intenção durável. Fechamento financeiro e callbacks arbitrários continuam distintos de `Future.done`.

A #127 passou 371 testes focados em cada Python 3.11/3.13, sem xfail nesse recorte. O teste antes marcado como strict xfail agora exige recuperação efetiva. A matriz inclui Service/Core/SQLite temporários, timers imediatos ou recusados, interrupção durante arming, cancelamento, sucessão, recuperação, progresso concorrente e preservação dos contratos de submissão. São clientes sintéticos; incidência em produção e fechamento financeiro tardio não foram medidos por esses testes. [Relatório](history/reviews/2026-09-14-autoresume-readiness.md).

**Próximo:** o plano vivo do [objetivo #108](https://github.com/marcelusfernandes/lohra/issues/108) segue para usage tardio (#111/#112, parent #10) e os demais recortes triados. Uma investigação confirmada não equivale a uma correção entregue: #7/#8/#10/#11/#17 continuam abertas para seus critérios restantes. O merge de uma fatia não encerra o objetivo amplo.

#86 aguarda pelo menos 50 insights reais no profile relevante. A nova medição, somente leitura e incluindo WAL, encontrou 4 linhas em 33 bancos inspecionados; os sinais continuam de QA. O segundo produtor existe, mas o volume ainda é insuficiente. #70 permanece bloqueada por falta de reprodução atual da falha histórica de ordem; sucessivos CIs verdes não a encerram. Essas lacunas não bloqueiam tarefas independentes.

A preparação do eval E0 (#92) pode avançar, mas sua aceitação comportamental e os dependentes #93–#96 exigem a matriz real prevista; testes fake não medem aderência de modelo. As avulsas #97–#99 não dependem dessa medição. Waves 5/X mantêm a decisão de stack da TUI como pendência própria. Não há nova publicação PyPI, migração de dados pessoais ou inferência paga implícita nesta continuação. Decisões já aceitas, inclusive a revisão por agente isolado usando a mesma conta GitHub (#102), permanecem válidas.
