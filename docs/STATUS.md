# Lohra — estado do projeto (histórico por fase e por wave)

Este arquivo é a memória narrativa do projeto: o que cada Fase/Wave entregou, o que foi validado ao vivo, o que ficou
pendente com custo nomeado, e o que vem a seguir. Instruções de trabalho ficam em `CLAUDE.md`; specs em `docs/specs/`;
decisões e evidência por data em `docs/history/`. Atualize a seção da rodada e o **Próximo** a cada release.

## Estado atual: Fases 0–10 + CC-Parity completas · Waves 4/6/7 + 7.5 completas · Wave 8 aberta (investigação feita, épicos propostos)
Tags git por fase: `phase-0`..`phase-5`. Backend com 2767 testes, 95% cobertura, ruff limpo. Publicada no PyPI (`pip install lohra`, 0.0.19).
- **0** esqueleto & contratos · **1** agent core (chat + streaming + CLI `lohra chat`) · **2** tools (fs/terminal), approval gate, SessionDB · **3** gateway FastAPI WS+REST + app desktop Tauri/React · **4** memory + skills (self-improving): MemoryStore, SKILL.md, SOUL.md, session_search (FTS5), snapshot frozen.
- **5 (completa):** metade A — ContextCompressor + AuxClient + compactação preflight no loop + lineage split no `lohra chat`. Metade B — `delegate_task` (subagents isolados: contexto fresco, caps pai=90/filho=50, depth 1, concorrência 3, auto-deny no filho; `lohra/agent/delegate.py`). Lineage split no gateway (evento `session.forked`, filha reusa Agent, pai despejado/bloqueado, busy-lock compartilhado; frontend faz rebind do `$sessionId`) + `compression_locks` (lock cross-process na tabela SQLite, PK single-winner + lease TTL, `busy_timeout` + back-off WAL).
- **6 (em andamento):** **Mais providers** ✅ — transport `chat_completions` (protocolo OpenAI; 11 providers: anthropic, openai, openrouter, deepseek, groq, together, gemini [endpoint OpenAI-compat do Google], xai (grok), glm (zhipu), kimi (moonshot), ollama [keyless via `requires_api_key=False`]) + `OpenAIClient` + `build_client(profile)` + CLI/gateway genéricos. **MCP client** ✅ — conecta servidores MCP (`~/.lohra/mcp.json`), expõe tools como `mcp_{server}_{tool}` no registry; ponte sync↔async, stdio+http (dispatcher `connect_session`). `lohra/mcp/`. **Servidor OpenAI-compatível** ✅ — `lohra serve`: `POST /v1/chat/completions` + `POST /v1/responses` (Responses API; objetos/eventos validados contra o SDK `openai` real), `/v1/models`, `/health`, auth Bearer; modo relay (default) + **modo agêntico** (`--tools <allow-list>`, allow-list gateia execução). `lohra/server/`. **Cron / jobs agendados** ✅ — `~/.lohra/cron/jobs.json` + matcher próprio (once/interval/cron) + scheduler em thread (dashboard) + tool `cronjob` interceptada + `lohra cron <…>`; jobs rodam como agente fresco sem tools, persistem sessão `source="cron"`. `lohra/cron/`. **Terminal embutido no desktop** ✅ — `portable-pty` (Rust) spawna o shell num PTY, comandos `pty_open/write/resize/close` + eventos `pty-output`/`pty-exit`; `TerminalPanel` (xterm.js) com toggle no header. `desktop/src-tauri/src/pty.rs`, `desktop/src/Terminal.tsx`. **Vision** ✅ — tool `vision_analyze` interceptada: imagem (`path` local→data URI base64 / `url` remota) + prompt opcional → análise textual de um modelo vision-capable (`make_vision_runner`, one-shot). Formato canônico = parts OpenAI (`image_url`), convertido pelos transports (chat_completions passa as parts; anthropic_messages traduz `image_url`→bloco `image`). Wirada no CLI/dashboard, excluída de subagentes/server. `lohra/vision/`. **Image_gen** ✅ — tool `image_gen` interceptada (prompt + `size`/`n` opcionais) → gera via OpenAI Images API (gpt-image-1), salva PNGs em `~/.lohra/images/` e devolve os paths. Capacidade `generate_image` no `ModelClient` (base levanta erro limpo; `OpenAIClient` chama `images.generate`, decodifica `b64_json`); `size` enum (só tamanhos válidos do gpt-image-1), `n` clampado 1–10. Só funciona no provider OpenAI (Images API). `lohra/imagegen/`. **Browser tools** ✅ (search + fetch) — `web_fetch` (httpx → texto legível via `html.parser`, recusa não-texto) + `web_search` (backend plugável `SearchBackend`, default keyless DuckDuckGo; distingue `SearchUnavailable` de zero resultados). Tools **normais** (stateless, como fs/terminal) → rodam em subagentes e gateáveis pela allow-list do server. **Guard SSRF na camada de fetch** (`validate_public_url`: só http(s), recusa loopback/privado/link-local/metadata/IPv4-mapped; redirects à mão revalidando cada hop; cap de bytes/chars). `lohra/web/`. **Profiles isolados** ✅ — `LOHRA_PROFILE` (ou `lohra --profile <nome>`) re-rooteia **todo** o estado sob `~/.lohra/profiles/<nome>/` (memória, skills, sessões/state.db, cron, mcp.json, imagens). Fonte única: `lohra_home()` resolve o profile (todo subsistema já passa por ela → isola por construção); `lohra_base()` é o root independente. Sem profile → exatamente `~/.lohra` de hoje (backward-compat, nada migrado). Validação anti-traversal (`validate_profile_name`) enforçada no read de `LOHRA_PROFILE` + na flag. CLI: `--profile` + `lohra profile list|create`. `lohra/memory/paths.py`. **Self-update (backend git-pull)** ✅ — `lohra update`: resolve o repo pela localização do pacote (`lohra.__file__`, não o CWD), pré-checks de estado de 1ª classe (tree sujo→aborta, sem upstream/detached, diverged≠erro, up-to-date) + `git pull --ff-only`; reporta arquivos + restart-to-apply; `pyproject` mudou→dica `pip install -e backend` (remédio que funciona, não re-run). `--check` (fetch+behind) e `--reinstall` (pip -e). Git/pip atrás de runners injetáveis (testável offline). `lohra/selfupdate/`. ⏳ casca Tauri depende de packaging. Tudo validado ao vivo. **Packaging (bundle macOS local)** ✅ — backend congelado (PyInstaller onedir) como **resource** do Tauri (`Contents/Resources/lohra-backend/lohra`); `backend.rs` resolve o sidecar (release: falha alto; dev: PATH). 4 gotchas do freeze em `docs/history/PACKAGING.md` (pathex, deps uvicorn[standard], onedir, **`lifespan="off"`**). **UI de settings da API key**: app do Finder não herda env do shell → backend lê `~/.lohra/.env` no startup (`lohra/config/`); painel de Settings grava via `config.rs` (`provider_status`/`save_provider_key`, chmod 600) + `restart_backend` + gate no boot. `.app` roda standalone, chat funciona end-to-end (validado ao vivo). `desktop/scripts/build-macos.sh`, `backend/packaging/`. ⏳ não-verificado (recursos externos): assinatura/notarização, appimage/msi, CI.

## Fase 7 — Orquestração de sessões paralelas (em andamento)
A Lohra-agente spawna sub-sessões paralelas, injeta steer e colhe async. Um core, dois consumidores (tool do agente + métodos WS). Spec em `docs/specs/06-orchestration.md`.
- **Milestone A** ✅ (validado ao vivo): `OrchestrationCore` (`lohra/orchestration/core.py`) — sub-sessões **independentes** (Agent isolado de subagente, `on_compaction=None`), `ThreadPoolExecutor` teto configurável; `spawn`/`steer`(busy→inbox, idle→novo turno)/`collect(wait)`/`cancel`/`shutdown`. Steer no loop (`run_conversation(inbox=…)` drena entre iterações → `<system-reminder>` no tail, Invariante #1 ok). Tool triad interceptada `spawn_session`/`steer_session`/`collect_session`, wirada CLI+gateway, excluída de subagentes/server. core 90% / tools 100%.
- **Milestone C** ✅ (validado ao vivo): `delegate_task` retomável — reusa o `OrchestrationCore` (não mais ThreadPool descartável); cada task vira sub-sessão persistida (`source="orchestration"`, filtrada do `list_sessions`), resultado carrega `sub_id`, e `resume_id` continua um filho existente (steer→collect). `DelegateTaskTool(core, parent_session_id)`; `build_session_dispatch` perdeu o param `child_factory`.
- **Limpezas** ✅ (3/3): inbox migrou pra `GatewaySession` (`enqueue_steer`/`drain_steers`; core perdeu o dict por sub_id) + eviction no `OrchestrationCore._children` (cap 200, só terminais) + parent linkage real no gateway (`AgentFactory` recebe `session_id`, fechado no M6 do CC-Parity).
- **Limites configuráveis** ✅: `resolve_limits` (em `orchestration/core.py`) — concorrência via `--max-parallel` (chat) ou `LOHRA_MAX_PARALLEL`; registry via `LOHRA_MAX_SUBSESSIONS`; flag > env > default (4 / 200). Inválido → default. Wirado no `lohra chat` e no dashboard (env-only).
- **Milestone B (métodos WS) ADIADO** — sem consumidor (o desktop não chama `prompt.background`/`session.steer`); o payoff agent-facing está em A+C. Requer restructure arriscado do WS. Ver `docs/ROADMAP.md`.

## Fase 8 — Harness de dynamic workflows — COMPLETA
A Lohra-agente **autora e roda dynamic workflows** (DAG declarativo typed, sem executar código autorado) com o rigor do Claude Code. Spec `docs/specs/07-workflow-harness.md`. Pacote `lohra/workflow/`.
- **A** ✅ — modelo + validador (`nodes`/`schema`/`refs`): node-types fechados; refs path-only single-pass (guard injeção 2ª ordem); `validate_spec` → WorkflowSpec|ValidationError didático (nunca levanta), e rejeita tipos válidos-mas-sem-strategy.
- **B** ✅ — `engine` (interpreter, ordem topológica, engine-fault isolado) + strategies `agent`/`parallel` + **leaf sandbox** (`sandbox.py`: fs path-allowlist deny-by-default + egress allowlist + taint; policy do operador, não do spec) + `budget` (fan-out/lifetime, nunca unbounded).
- **C** ✅ — structured output: `validation.py` (jsonschema) valida a saída do leaf; mismatch → steer de correção na mesma sub-sessão (bounded); falha → null.
- **E** ✅ — nós de rigor: **verify** (adversarial: N skeptics refutam, maioria mata), **judge_panel** (N attempts → judges → vencedor sintetizado), **loop_until_dry**.
- **F** ✅ — tool surface `run_workflow`/`workflow_status`/`workflow_cancel` (interceptada): `WorkflowService` valida → core sandboxed por run → roda em background → run_id na hora. Wirada CLI+dashboard, excluída de subagentes/server. Sandbox validado no caminho REAL (teste de integração + discriminador).
- **D** ✅ — pipeline **no-barrier**: core ganhou `spawn(on_done=…)` (hook não-bloqueante, 1x por sub-sessão, fora do lock, não no busy-handoff); `run_pipeline` encadeia cada item pelos stages independentemente (on_done não-bloqueante: collect wait=False + validate + re-spawn; barreira Event.wait no pool separado do service). Testes que definem correto: no-barrier + throughput (≤ pool_width, sem deadlock). Drop-on-invalid (v1).
- **G** ✅ — resume/cache content-addressed: tabela `workflow_node_cache` (key por conteúdo, run-scoped, cross-run OFF §6.3); get-or-spawn por célula (scalar + pipeline per-(item,stage)); **só completions são cacheadas** (dead/invalid re-spawnam no resume — LLM estocástico). `resume_run_id` re-roda reusando o cache; partial-resume só re-spawna o que faltou. budget lifetime guardado (sem cap silencioso).
- **J** ✅ — self-improvement: `rollup.py` (rollup rico, `null_rate` first-class) + `library.py` (run problemática → **prior** em `~/.lohra/workflows/insights.md` com lock+dedup+cap, **não** o MEMORY.md/prompt; run limpa → **template** validado). Tool `workflow_templates` (list+get, devolve {templates, insights}) + nudge no `run_workflow`. Run cancelada não grava prior.
- **H** ✅ — nesting (`workflow` node): recursão de engine determinística, `nested_engine()` compartilha core/budget/cache/loader (sandbox/budget não escapáveis), `MAX_WORKFLOW_DEPTH=1`; `fold_nested()` dobra métricas aninhadas no rollup do pai (J não certifica composto quebrado). **Todos os 7 node-types executam.**
- **taint real** ✅ — `agent/taint.py`: TaintTracker (sticky/sessão) + taint_wrap no dispatch; turno que ingere web/MCP → `run_workflow` roda leaves sem fs+egress. Discriminador no caminho real.
- **I — forced `tool_choice`** ✅ — `build_kwargs(tool_choice=…)` byte-idêntico no default; node `tool_less:true` + schema → `core.spawn(configure=…)` seta `agent.forced_tool` → loop força a synthetic tool (args=resposta), provider ignora → §5.1. Turno replay-safe. Fix do mix-up `schema:"nome"` vs `schema_ref` (resolve_schema coage + validador rejeita irresolvível).
- **Polish** ✅ — `aggregate_tokens` + `forcing_fallbacks` no rollup/priors (usage do leaf → core → RunResult, lock nas escritas off-thread do pipeline); retry-por-stage no pipeline (re-spawn fresco com correção, bounded). Tokens validados ao vivo.
- ~815 testes, ruff limpo. **Fase 8 completa (A–J + polish), tudo validado ao vivo.**

## Fase 9 — Consciência de projeto (instruções + skills do projeto) — COMPLETA
Rodando num projeto, a Lohra lê/segue AGENTS.md/CLAUDE.md e descobre/lê/edita/usa as skills do projeto. Implementada com **ultracode** (cada fatia gateada por review adversarial multi-agente antes do commit). `lohra/project/discover.py`, `lohra/safeio.py`.
- **A1** ✅ — `discover.py`: acha a raiz (VCS/agent root tem precedência sobre build-manifest — fix do dogfood `cd backend`), lê AGENTS.md/CLAUDE.md → `context_files` + `environment_hints` no prompt congelado. `safeio.read_text_bounded` (symlink/não-regular rejeitado, byte-bounded) — sem OOM/hang/exfil. Wirado CLI+dashboard.
- **A2** ✅ — `SkillStore(home, extra_roots)` escaneia `.claude/skills`/`.lohra/skills` do projeto (precedência sobre home), index rotula `(project)`; create/delete **home-only** (delete nunca apaga repo do usuário). `discover_skill_roots`.
- **A3** ✅ — `skill_manage`: `update` edita in-place onde a skill vive (projeto→projeto), `create scope=project`; `_under_roots` guarda escrita contra symlink-dir escape; OSError→tool_error; platforms round-trip.
- **A4** ✅ — "usar" emerge de A1+A2. E2E ao vivo: projeto com AGENTS.md (🦊) + skill pirata → Lohra usou a skill e seguiu o AGENTS.md. ~854 testes.

## Fase 10 — Auth por subscription (OpenAI/Codex SOMENTE) — ✅ COMPLETA, validada ao vivo (ToS-gray)
Opt-in, ToS-gray. Dois caminhos de login: **próprio** (`lohra auth login`, OAuth device flow + store próprio + auto-refresh) com precedência, e **reuse** do Codex CLI (`~/.codex/auth.json`, sem refresh). Construída com **ultracode** (design+pesquisa panel + review adversarial por fatia; constantes = fatos públicos citados ao repo `openai/codex`). `lohra/subscription/`. ~929 testes.
- **B1** ✅ credential subsystem: lê `~/.codex/auth.json` (safeio bounded/symlink-safe), store `~/.lohra/auth.json` (opt-in + `acknowledged_tos_risk` hard-gate fail-closed, chmod 600, sem token), expiry via JWT exp. Refresh em memória **não** reescreve o Codex (rotação→revogação); reprs redatados.
- **B2** ✅ Responses-API transport+client: o backend Codex só fala Responses. `store=false` + `stream=true` (exigidos, descobertos ao vivo); `ResponsesClient` sempre streama e remonta a Response dos `output_item.done` (o `completed` vem vazio sob store=false). Vision via `image_url→input_image`. `response.failed` levanta erro (não engole).
- **B3** ✅ wiring + `lohra auth` (status/enable/disable, aviso de ToS): run_chat **e dashboard** resolvem subscription ANTES do provider (alcançável sem API key); model default = o da config do Codex (`read_codex_model`, fallback gpt-5.5); **`lohra serve` RECUSA** (não vaza o token).
- **B4** ✅ robustez (token nunca no env; reuse do Codex não refresca — rotação corre com o Codex).
- **B5** ✅ (gap #1 FECHADO): first-party OAuth — `oauth.py` device flow (auth.openai.com, mesmo client_id, account_id via JWT claim, User-Agent obrigatório); `token_store.py` (`~/.lohra/oauth.json`, escrita atômica 0600, repr redatado); `resolve` refresca 5min antes de expirar + persiste o family rotacionado (SEGURO — token próprio; race→re-lê o vencedor). `lohra auth login`/`logout`.
- **Reasoning replay** ✅ (paridade opencode): `include=["reasoning.encrypted_content"]` + captura em `provider_data` + re-injeção no input (só items com encrypted state) → continuidade de raciocínio sob store=false.
- **Validado ao vivo** (subscription do usuário): chat · single/chained tools · usage/tokens · dashboard · **workflow fan-out** · **reasoning continuity** · **vision** · **login próprio + auto-refresh**. Review ultracode/advisor limpo em cada fatia.
- **Não-verificado** (nomeado): streaming WS no desktop sob subscription; fan-out grande (rate-limit — degrada gracioso via `response.failed`). `image_gen` falha gracioso sob subscription (Responses não faz Images API).

**Comparação com opencode** (prior-art): núcleo idêntico (Responses+store=false+bearer+mesmo client_id) E agora paridade no que faltava — reasoning replay + OAuth/store próprios com refresh transparente (B5).

## CC-Parity do workflow harness (2026-08-26/27) — COMPLETO, validado ao vivo
Campanha guiada pelo gabarito real dos dynamic workflows do Claude Code (scorecard 15 capacidades). Docs: `docs/history/ROADMAP-CC-PARITY.md` (M1–M7) · `docs/history/BACKLOG.md` (WF-1..30) · `docs/history/reviews/` (review 15-agentes + dogfoods). Suíte **959→1273**, 94% cobertura.
- **M1–M2 fail-closed**: falha NUNCA é silenciosa — fault com causa em todo caminho (container `${ref}`, upstream-null vira fault e não a string "null", verify fail-closed, status honesto `complete|degraded|failed|cancelled|paused`); done-path do pipeline blindado; liveness guard no resume.
- **M3+M3.5 autoria**: `SpecIssue.example` renderizado no erro; RUN_GUIDANCE com os node-types; **skill builtin `workflow-authoring`** (tier builtin no SkillStore: projeto > home > builtin, update = copy-on-write pro home) com testes de contrato anti-drift (fences validados de verdade).
- **M4 resiliência**: quota→`paused` (classificação por TIPO de exceção, nunca prosa) + auto-resume (backoff, cap 5, cold-start rearm); cancel real; cancel-on-timeout; `timeout`/`retries` por nó.
- **M5 token budget**: gate soft pré-spawn + gate de fan-out estimado nas barreiras; paused sem auto-resume (raise-only no resume, erro sugere valor); custo por célula (`workflow_node_cost`) + cumulativo (`workflow_run_spend`); `{total,spent,remaining}` até mid-run.
- **M6 operabilidade**: progresso mid-run por nó (engine vivo), `workflow_list`/`workflow_pause`, notificação via inbox de steer; AgentFactory recebe session_id (materializou o parent linkage adiado da Fase 7).
- **M7**: node-types 7→**10** (`gate` semântico, `completeness_check`, `checkpoint` humano journaled); tiers de operador (`~/.lohra/workflow_tiers.json`); contrato `NODE_SPECS ≡ STRATEGIES ≡ skill` testado; `fs_allow` com modo ro/rw.
- **WF-24..30 (dogfood via Codex)**: args reidratados no resume; cell-cache nos 5 node-types órfãos (resume re-pagava 93,6k → **0 tokens**, comprovado ao vivo); priors citam faults reais; `faults_total` cumulativo; **WF-29 estado durável** (`workflow_run_state` + lease com heartbeat + cold-start rearm — checkpoint funciona ENTRE processos/turnos do CLI, validado ao vivo com humano aprovando via o próprio checkpoint); WF-30 (race stop×tick do heartbeat, achada pela própria Lohra revisando o código do dia).
- **Pendências nomeadas**: WF-11 (worktree/leaf) adiado com racional; 3 candidatos do review da Lohra a triar (`docs/history/reviews/2026-08-26-dogfood-codex.md`); `builtin_root` sob PyInstaller não re-verificado. Skill `use-lohra` avisa o footgun de custo (profile novo não herda subscription).

## Standalone + PyPI + Live View (2026-08-26/27) — COMPLETO, validado ao vivo
O backend virou **runtime instalável e publicado**: `pip install lohra` (PyPI, 0.0.1→0.0.4).
- **Standalone**: wheel py3-none-any (Python 3.11–3.13; zero imports Unix-only; anthropic embutido nas deps base); LICENSE MIT; skill builtin no wheel; versão single-source (`importlib.metadata` — a fonte é o pyproject); `lohra update` fora de git ensina o remédio pip. `docs/STANDALONE.md`.
- **Kit de delegação**: `lohra/skills/export/use-lohra/` (package data, SEPARADO do builtin — skill p/ orquestradores não entra no índice da Lohra) + `lohra skill export <name> [--to DIR]`; anti-drift: cópia empacotada ≡ `docs/skills/use-lohra` (teste).
- **Validado ao vivo**: venv virgem do PyPI (macOS) e **Windows real** (`pip install lohra` + chat funcionaram — primeira execução fora do macOS). Codex headless bloqueado por política enterprise do usuário (interativo ok).
- **Live view** (`workflow/events.py`, `liveview.py`, `liveview_tui.py`, `watch.py`): o operador enxerga o run — DAG impresso na aceitação do spec, eventos node/items/fault/done ao vivo no stderr (faults NA HORA; custo escala item a item; 100% harness, zero LLM), modo TUI in-place (LiveBlock puro + LivePainter ANSI, `isatty`+`LOHRA_LIVEVIEW=fancy|plain|off`, sanitização de control chars — spec é autorado pelo agente), progress durável (`progress_json` — status/list cross-process de run terminada mostra nós reais), **`lohra workflow list|watch`** (espectador sem LLM, zero tokens; watch sai em terminal/sumida/stale). Validado ao vivo pelo usuário no terminal (2 rodadas) + PTY real.
- Suíte **1377**, 94% cobertura. Ideia anotada (pedido do usuário): "tempo real" — relógio por nó + tokens estimados `~` durante o voo dos leaves (estimativa NUNCA entra na contabilidade).

## Waves 4→7 (0.0.10→0.0.13) — auditoria, fencing, cancel/reserve, supervisão ativa
Pós-CC-Parity o trabalho passou de Fases para **Waves** rastreadas por GitHub milestones/issues, cada épico com aceite por avaliador independente. **Wave 4 — auditoria dos nodes** (0.0.10, épico OBS **implementado pela própria Lohra**, 5 issues): `lohra workflow audit <run_id>` + tool `workflow_audit`, ledger durável metadata-only com gaps honestos (tombstones/`$compacted`), `LOHRA_AUDIT` switch; spec `docs/specs/08`. **Fencing cross-process** (0.0.11, #12): `workflow_run_fence` monotônico por aquisição — toda escrita sob ownership apresenta a cerca; dono obsoleto recusado, nunca corrompe; + auth REST do gateway e rejeição de tool-call stream incompleto (PR #33). **Cancel/reserve** (0.0.12, #8/#14): cancel de leaf enfileirada dispara terminal na hora (status `cancelled`), `on_lease_lost` aborta a execução do dono expulso, `Budget.reserve()` atômico com refund só do que nunca rodou — fundação da Wave 6. **Wave 6 — supervisão ativa** (0.0.13, 6 issues SUP-01..06, **épico liderado pela Lohra**; quota 429 no meio → coautoria declarada da fatia final): doutrina vigiar→diagnosticar→adaptar→retomar com fronteira agente×humano e freios (1 contorno por `(run,causa,target)`, 3/run, K=2, allowance min(6k,25%)) + 37 contratos anti-drift; `workflow_status` com proveniência honesta; steering endereçado por identidade quíntupla (só ocorrência viva, lido entre iterações, prompt congelado provado por identidade de objeto); pivô = spec adaptado no mesmo `resume_run_id` reusando cache; aprendizado no erro (classificador fail-closed, notices duráveis claim/ack com dedup/cap/TTL, dead-turn notices inclusive nos descartes invisíveis, persistência de turno TRANSACIONAL `save_messages`); gate E2E CONFIRMADA (matriz cross-feature sem bug de produção + probes adversariais ao vivo — texto segura manchetes, não cláusulas sutis → #36). Módulos novos: `lohra/state/{notices,insights}.py`, `lohra/workflow/{failure_taxonomy,steering,supervision}.py`, `lohra/agent/notices_overlay.py`. **Wave 7 — robustez operacional (COMPLETA, 0.0.14→0.0.17, milestone fechado 4/4)**: #38 janela de contexto OpenRouter no preflight (fechada antes) · #34 audit sink — causa raiz era MIGRAÇÃO faltante (tombstones.next_seq) mascarada de contenção; migração + busy_timeout 250ms do operador (LOHRA_AUDIT_BUSY_TIMEOUT_MS) + warnings agregados por tempo + drain final de markers (0.0.16) · #39 rastro de acks — tombstones bounded `notice_trail` (atômicos com o DELETE; acked/expired/evicted com token; cap 64 + TTL 30d + varredura global) + `lohra notices <session_id>`; contrato claim/ack intocado; tombstone-sobre-flag registrado em docs/history/2026-08-31 · #40 morte por sinal — `agent/signals.py`: SIGTERM/SIGHUP viram TerminatedBySignal e o epílogo NORMAL publica dead-turn notice + envelope --json + morte fiel 128+N (die_by_signal); desarme-no-1º-sinal, SIG_IGN/nohup respeitado, defer_signals nas janelas de persistência, canonical_done antes do ack (nunca fato falso); SIGINT sob --json com a mesma semântica; review adversarial (ultracode) achou e matou o join do pool de tools segurando o unwinding (0.0.17, validado ao vivo com kill real). Fillers fechados: #24 (watch/audit por prefixo curto), #35 (--provider sobrepõe subscription sob auto). Abertos: #37, #41; #36 gateado por decisão de produto. Também na 0.0.15: custo em USD no envelope --json (`lohra/pricing/`: fail-closed, api_equivalent para subscription, usage_total somando o turno inteiro) + skill use-lohra com cost/override/notices (anti-drift cobre a cópia .codex).

**Wave 7.5 — fatias paralelas verificadas-no-código (2026-09-01, 0.0.18, branch `integration/wave-7.5`, 5 worktrees em `../lohra-wt`, agentes opus/sonnet coordenados + review adversarial nas fatias de risco)**: #4 leaf sandbox nega `terminal`+`mcp_*` por default (exfil via shell/MCP estava aberta até em run tainted; opt-in SÓ do operador via `workflow_policy.json`/`LOHRA_LEAF_ALLOW_TERMINAL`/`LOHRA_LEAF_MCP_ALLOW`; definitions filtradas) · #49 lint de DAG desconexo (`warnings` no aceite) + #2 `depends_on` malformado rejeitado (era descartado em silêncio e reordenava) · #47 só observabilidade (não era deadlock: notice para sessão sem próximo turno + `watch` em loop infinito em `paused`) — `watch` sai em pausa sem auto-resume, `pause_reason` em list/watch, campo `workflows` no envelope `--json`; teto de operador PENDENTE (decisão do dono) · #48 reescrita (leaves streamam; `LEAF_TIMEOUT=120s` morde antes dos 600s HTTP; retry M4 não re-executa leaf morto) — `LOHRA_PROVIDER_READ_TIMEOUT` (default byte-idêntico) + `error_kind="timeout"` + fault didático · #8/#42-B quiescência após cancel (`workflow/quiescence.py`, teto 5s `LOHRA_CANCEL_QUIESCENCE_S`, fault diz settled × STILL RUNNING) + **#15 decidido: `required: true` real** (`workflow/required.py`; abort → `failed`, restantes `skipped` não-nulados, fora do cell_hash, nested sobe; `min_success_ratio` segue ignorado — spec ambíguo) + triagem dogfood-codex (i refutado; ii corrigido: cancel não reescreve veredito terminal; iii confirmado, xfail estrito). **Validado ao vivo via Codex headless** (2026-09-02, `docs/history/reviews/2026-09-02-dogfood-codex-wave7.5.md`): 6 testes, todos com o comportamento esperado; T1 divergiu só na expectativa (leaf não VÊ `terminal` — filtro de definitions — em vez de receber a negação didática). Pendências nomeadas dos reviews (web_search fora de `egress_allow`, redirect×allowlist, taint_wrap no leaf, N×teto em fan-out, null_rate com skipped, auto-resume precoce) em `docs/history/2026-09-01-wave7.5-parallel-slices.md`.

**Wave 8 prelúdio (2026-09-02, 0.0.19, integração LINEAR sem merge commits — o `release.sh` espelha por cherry-pick)**: decisões do dono que faltavam viraram código — **#15 fechada**: `min_success_ratio` removido do schema com erro didático `min_success_ratio_removed` (nunca `unknown_field`) apontando `gate`/`completeness_check` marcado `required` · **#47 fechada (parte 2)**: teto de tokens pré-autorizado pelo OPERADOR (`workflow/operator_budget.py`: `--token-budget-cap` > `LOHRA_TOKEN_BUDGET_CAP` > sem teto; efetivo = min(spec, cap), cap sozinho quando o agente não pede; resume clampado; proveniência `token_budget: {total, source, operator_cap}` no aceite; hints apontam o operador; teto é POR RUN — teto process-total é pendência nomeada; review adversarial: sem bypass, 4 gates textuais/proveniência aplicados) · #36 segue gateado. **Wave 8 aberta como investigação read-only** sobre a run real `42abc3eb…` (banco do profile lohra-notion-v4), 3 relatórios em `docs/history/reviews/2026-09-02-wave8-investigation.md` e comentários em #42 #43 #44 #45: cache CORRETO (0 reexecuções incorretas; falta explicabilidade — preview de blast radius pré-resume é o épico S de maior valor); zumbi do cancel agiu 156 s após o sucessor (causa: `loop.py:426` despacha tool calls sem checar interrupt — épico S), dano não observado; 3/5 artefatos de células mutados por leaf vivo, prejuízo zero (spec sem `${ref}`); pivô do v4 foi HUMANO verbatim (SUP-01 funcionou), custo real = 55% dos tokens fora de célula por degradar em silêncio — opções A/B/C para o dono (recomendação: pausar `route_fault` antes de envelope de rotas). 10 épicos ordenados na tabela do relatório. `workflow_insight_candidates` vazia no run = evidência para a Wave 9.

## Orquestração — extensões (na main, validadas/revisadas)
- **`lohra chat --json`** ✅ — envelope estruturado completo p/ orquestração (input/output/reasoning/tool_calls com result/model/usage/stop_reason/error). stdout = SEMPRE 1 JSON parseável (até em erro: error_envelope; `ensure_ascii=True`; streaming off). `lohra/agent/result_json.py`. Validado ao vivo. Skill pronta: `docs/skills/use-lohra/`.
- **Multi-model + reasoning effort por subagente** ✅ (same-provider) — `agent/overrides.py make_configure` via o hook `core.spawn(configure)`; campos `model`/`effort`/`provider` no workflow node + `delegate_task` + `spawn_session`; `Agent.effort` + `build_kwargs(effort=…)` (responses→`reasoning:{effort}`, chat→`reasoning_effort`, anthropic no-op). Effort validado ao vivo no Codex. Fechou o footgun do campo declarado-mas-ignorado.
- **Cross-provider delegação** ✅ — `agent/client_pool.py ClientPool`: orquestrador no provider X, subagente/leaf em provider DIFERENTE (Claude→GPT→Codex). Build+cache por provider (lock), borrow do client do pai (fecha só os próprios), gate de escalação de subscription (`openai-codex` só via `subscription_active`), provider-error fail-isolado, swap atômico provider+client. Cross-provider ao vivo precisa de 2ª key do usuário; same-provider + subscription validados. ~959 testes.


**Próximo:** Wave 9 — memória operacional e aprendizado no erro (milestone 11, 4 épicos #50-53: gatilhos de candidate com agência + baseline de recorrência, priors específicos+consumo, momento da dead-turn notice, destilação pai←filho — evidência: 1 memória em meses, 0 insight candidates, priors boilerplate) e Wave 8 — topologias/contratos do DAG (milestone 10, épicos #42-45 semeados do feedback da run lohra-notion-v4; avulsas #46-49: USD por provider, budget headless, read-timeout, lint de DAG). Decisão de stack das Waves 5/X (TUI×REPL, textual×TS-sobre-gateway, compartilhada); decisões do dono pendentes: #36 (enforcement por default?); #45 mismatch de manifesto (re-spawn/fault/notice); #43 opção A/B/C (recomendação C→B); teto process-total de budget. Próximo passo sugerido: épicos 1-5 da tabela do relatório da Wave 8 (todos S). Waves 0–3 (campanha de release-confidence, 13 issues investigação-primeiro) prontas para atacar em ordem. Fillers: #37 (batching de tool calls), #41 (janela de contexto ao vivo). Pendências menores seguem: gateway de mensageria (opcional), distribuição/CI do packaging, cross-provider ao vivo (2ª key). SIGKILL/marcador-de-sessão-aberta adiado com custo nomeado (docs/history/2026-08-31-wave7-notices-signal.md).
