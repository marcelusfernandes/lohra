# Committed wheel gate — #114

2026-09-14. Claimed base: `58b2584829f3d7a9f273c25d9e5ad06aa70b3567`.
Implementation and locally tested source commit:
`763ccdf2465d8cb3a9456ce4733bcab736de6970`; tree
`e9868b0144168d093732c6588b11269fd3fa3580`. This evidence-only follow-up does not
claim that the implementation gates ran on its new documentation commit. CI
must build the actual published checkout, recording its own identity and timing.

The existing Ubuntu backend jobs retain editable install, Ruff and full pytest.
They now include `ci` in Ruff and run `python -m ci.wheel_gate`. There is no new
required check, OS matrix, dependency, version change, runtime/skill edit or
release action. The gate builds a wheel directly; sdist work in #147 is separate.

## Implementation and acceptance

The helper first requires package/build/helper/CI edits to be committed, then
archives HEAD into a temporary directory. Its expected paths/hashes come from
**all tracked files under backend/lohra**, not the wheel or a hand-maintained
module/asset list. The archive copy avoids stale editable/build debris and
preserves the checkout. Source SHA/tree and PR head/base are separate evidence:
the CI virtual merge is not renamed as the PR head.

`wheel_contents` checks archive/RECORD membership, hashes and sizes, then package
completeness/content and metadata version. A valid RECORD cannot conceal a file
missing from the independently inventoried source. New nested modules and new
asset extensions are automatically expected. Empty inventories fail.

The wheel is installed with normal dependencies into a new venv without
system-site-packages. `installed_smoke` runs with that venv's `python -I`, proves
lohra/CLI/distribution origin in its purelib and checks every expected installed
byte, version and entrypoint. Pip-generated bytecode and metadata are allowed
without confusing them with source requirements. It loads real packaged builtin
skills. Direct CLI controls cover version, help, export bytes and empty workflow
list; the no-provider entrypoint runs under a network-blocking launcher and must
return exit 2 with the specific JSON error, false completion and zero API calls.
No production provider resolver is patched; automatic Ollama discovery is blocked.

Each phase records status, duration, command/exit and log path; subprocess errors
and partial timeout output remain visible. The final JSON is saved and printed
for CI provenance. The independently measured total is not a sum of phases:
prepare-source contains its named git subprocess phases.

## Tests and observed results

Tests were written before the helper. The first collection failed because `ci`
did not yet exist; that is scaffolding evidence, not a runtime regression. The
initial focused matrix passed 59 cases with one skip in both Pythons. Two further
command-failure/timeout diagnostics cases were added; unused test imports found
by Ruff were removed. No real gate failure occurred in this implementation.

Final focused matrix: **62 distinct cases = 30 new + 32 existing**; **61 passed,
one skipped** on Python 3.11.15 (0.32 s) and 3.13.5 (0.39 s). The existing skip is
the gitignored local `.codex/skills` copy, absent from the public worktree. The
five files are `test_wheel_contents`, `test_wheel_gate`, `test_skill_export`,
`test_workflow_authoring_skill` and `test_smoke`. Initial/final runs and interpreters
overlap; they are not additional unique cases. Ruff over backend and diff-check pass.

Only after committing the implementation did each runtime run the real gate:

| Phase | Python 3.11 | Python 3.13 |
| --- | --- | --- |
| Isolated wheel build | 1.772 s | 1.873 s |
| Fresh venv | 1.804 s | 1.317 s |
| Normal dependency installation | 4.990 s | 4.815 s |
| Installed import/content check | 0.191 s | 0.255 s |
| Entire gate, measured independently | **10.163 s** | **9.898 s** |

Both independently built artifacts happened to have SHA256
`e28b90fc2afddb0877b3d27eff43e94ed213641c3be35b9513928a8cdc229443`
(724,880 bytes, 0.0.27). This is not a general reproducible-build claim. Each
contains **208 source package files (206 Python + two skills), 214 RECORD entries**,
and all source→wheel→installed hashes match. `httpcore>=1.0.9,<2` remains declared,
normal installation and `pip check` pass. Both unconfigured controls report zero
API calls and record the blocked network attempt.

Three additional artifact controls use copies of these actual candidate wheels:
missing `workflow/financial_settlement.py`, missing exported use-lohra SKILL.md,
and changed module bytes with the same version. RECORD is regenerated correctly
in each copy. Both interpreters reject the first two for missing package files
and the third for differing bytes. These are **three distinct adversarial cases**,
reported separately from pytest; no extra build or installation was performed.

## Reproduction and boundaries

From task-114/backend, each committed gate used its explicit runtime, with
runtime bin first in PATH and PYTHONDONTWRITEBYTECODE=1:

```sh
/tmp/lohra-wave10-py311/bin/python -m ci.wheel_gate \
  --output /tmp/lohra-114-gate-763ccdf-py311
```

The caller's PYTHONPATH identified the exact task-114/backend helper; the gate
removes it from every subprocess. Use a new output directory to repeat. Both
runtime directories preserve `result.json`, expected-package.json, the wheel,
venv, per-phase logs and negative-results.json. Top-level logs are
`/tmp/lohra-114-gate-763ccdf-py311.log` and `py313.log`. Unit/Ruff logs:
`/tmp/lohra-114-unit-final-py311.txt`, `py313.txt`, `/tmp/lohra-114-ruff-final.txt`.
The extra artifact controls are reproducible with
`/tmp/lohra114_candidate_negatives.py` and either gate output directory.

Build/install network access is normal dependency resolution; smoke has no
provider credentials, personal profile or inference. HOME/CODEX_HOME are preserved,
and temporary cwd/LOHRA_HOME/export isolate Lohra runtime state. Installer credential
isolation required the repair below; the initial green gate did not prove it. Timings are
macOS with available caches, not cold installs or Ubuntu CI measurements. The
old 205febd/75547e9 preparation artifacts remain distinct and untouched. Full CI
and independent review of the final published SHA remain coordinator gates;
parent #17 still owns wider native-platform and distribution coverage.

## Installer authentication repair after review

Review of `5ae951fe82c894e647fa8e55899e7ea017f8d810` identified a real gap:
`PIP_CONFIG_FILE=/dev/null` disables pip configuration but does not prevent its
requests auth layer from reading `HOME/.netrc`. The old environment allowlist
also discarded a caller's `NETRC=/dev/null`, making that caller's restriction
ineffective. Preserving HOME did not by itself isolate installer credentials.

Before changing the helper, seven new tests used **real pip command parsers and
PipSession**, synthetic homes/credentials, disabled sockets and no installation.
Both Python 3.11 and 3.13 produced **six REDs and one positive control**:

- Four wheel/install × absent/already-disabled caller NETRC cases attached
  synthetic Authorization to prepared pypi.org and files.pythonhosted.org requests.
- Two wheel/install 401 controls consulted the fake keyring provider. The actual
  pip path defaulted to `auto` and allowed prompts; the probe captured keyring,
  prompt and resend attempts without accessing a real keychain or network.
- The unisolated positive control detected the synthetic .netrc credentials,
  proving that a probe unable to observe auth would not pass unnoticed.

`clean_environment` now forces `NETRC=/dev/null`,
`PIP_KEYRING_PROVIDER=disabled` and `PIP_NO_INPUT=1`. HOME/CODEX_HOME remain
unchanged; build children and the new venv's normal dependency installer inherit
the restrictions. Inherited index/client-certificate/provider settings remain
discarded. No runtime, skill, dependency or packaging-format changes were needed;
#147/#148 remain outside this repair.

The first post-fix matrix preserved all behavioral assertions but exposed a test
diagnostic assumption: pip parses the environment's no-input value as integer
`1`, not the bool singleton `True`. Two cases failed only that identity check.
The JSON probe now reports `bool(options.no_input)`; absence of Authorization,
keyring access, prompts and resend, plus unchanged 401 identity, stay asserted.
That intermediate log is preserved separately from the six production REDs.

Final repair focus: **37 distinct cases = seven new auth tests + 30 existing gate
controls**, all passed on both Pythons (3.11: 2.81 s; 3.13: 2.96 s). Reruns and
interpreters overlap and are not extra distinct cases. Ruff and diff-check pass.
The old five-file partition above was corrected after collection only:
12 wheel-content + 18 wheel-gate tests were new; 8 skill-export + 14 authoring-skill
+ 10 smoke tests were existing. Original 61-pass/one-skip logs remain untouched.

Evidence: `/tmp/lohra-114-auth-red-py311.txt` and `py313.txt`, immutable pre-fix test
copy `/tmp/lohra-114-auth-red-test-5ae951f.py`, intermediate
`/tmp/lohra-114-auth-green-py311.txt` and `py313.txt`, final
`/tmp/lohra-114-auth-final-py311.txt` and `py313.txt`, Ruff
`/tmp/lohra-114-auth-ruff.txt`, and collection-only partition inventory
`/tmp/lohra-114-auth-prior-case-inventory.txt`. Tests prepare requests and feed a
synthetic 401; they do not claim real authenticated HTTP or native keychain
coverage. A fresh gate must build the committed repair before publication.
