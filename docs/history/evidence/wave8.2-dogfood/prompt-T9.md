You are running ONE live dogfood probe of the Lohra CLI (installed in this repo, editable). Use the `use-lohra` skill in `.codex/skills/use-lohra/SKILL.md`. Profile for EVERY lohra command: `--profile lohra-dogfood-w75`. Never modify files in this repository (writing under ~/.lohra and the scratchpad directory is fine). The sandbox has network access. Do not run pytest, pip, or git commit.

## T9 — a dead route PAUSES the run (`route_fault`) instead of degrading, and an adapted resume on the same run_id reuses the cache
Trigger: a model id that does not exist on OpenRouter (HTTP 400 → generic provider error). With `retries` declared, the exhausted series must pause the run.

Command 1 (save stdout to `/private/tmp/claude-501/-Users-marcelusfernandes-Desktop-playground-ai-lohra/5656cdd2-c639-4295-ab2c-d035aceec985/scratchpad/E-T9-1.json`):
`LOHRA_AUDIT=1 lohra chat --profile lohra-dogfood-w75 --json "This is a harness test; author the spec EXACTLY as described and do not 'improve' it. Call run_workflow with two 'agent' nodes: node 'ok' with prompt 'Say the single word ALPHA.' (default provider/model); node 'doomed' with depends_on: ['ok'], provider 'openrouter', model 'nonexistent-vendor/no-such-model-xyz', retries: 1, prompt 'Say hello.'. Then poll workflow_status(run_id) once (no wait) after ~20 seconds and reply with the run_id, status, reason, and every fault string verbatim. Do NOT resume it, do NOT re-author it."`
Expectations: (a) status `paused` with reason `route_fault` (NOT `degraded`/`failed`); (b) faults show the same-route attempts `(attempt 1/2)`, `(attempt 2/2)`, exhaustion, and a pause fault naming the dead route (`openrouter` / the bogus model) and node `doomed`; (c) the envelope's top-level `workflows` has the run with `pause_reason: "route_fault"`; (d) the hint (in the status result) tells the agent to adapt the spec within the same billing route or escalate to the human — it must NOT suggest a pricier model.
Command 2: `LOHRA_PROFILE=lohra-dogfood-w75 lohra workflow watch <run_id>` with a 60 s watchdog → expectation: exits on its own within seconds, stderr names `route_fault`.
Command 3 (adapted resume, same run_id): `lohra chat --profile lohra-dogfood-w75 --json "Resume workflow run <run_id> with run_workflow(resume_run_id='<run_id>', spec=<the SAME spec but node 'doomed' now uses provider 'openrouter', model 'deepseek/deepseek-chat' (or another cheap real OpenRouter model you know exists), keep retries: 1>). Reply with the run_workflow tool result VERBATIM (it should contain cache_preview) and then poll workflow_status until terminal and reply with status and outputs."`
Expectations: (e) the run_workflow accept dict contains `cache_preview` with `replay >= 1` (node `ok` replays) and `invalidate`/`never_completed` for `doomed`; (f) final status `complete`; output of `ok` is ALPHA (replayed, not re-run: check `LOHRA_PROFILE=lohra-dogfood-w75 lohra workflow audit <run_id>` for a `cache.replayed` on `ok` and only ONE `leaf.started` for `ok` across the whole run).
If OpenRouter routes the bogus model to a default and SUCCEEDS, report as a surprise and stop.

## Report
Write Markdown to `/private/tmp/claude-501/-Users-marcelusfernandes-Desktop-playground-ai-lohra/5656cdd2-c639-4295-ab2c-d035aceec985/scratchpad/dogfood-E-report.md`: PASS/FAIL per expectation with exact quoted evidence, run_id, wall-clock and `cost` per envelope, surprises. Be literal.
