This is a harness test; author the spec EXACTLY as described and do not "improve" it. Call run_workflow with a spec containing exactly one "parallel" node id "p" with exactly 3 branches. Branch 1 prompt: "Append exactly one line 'alpha was here' to the file notes.txt using write_file with mode append; reply OK.". Branch 2 prompt: "Append exactly one line 'beta was here' to the file notes.txt using write_file with mode append; reply OK.". Branch 3 prompt: "Append exactly one line 'gamma was here' to the file notes.txt using write_file with mode append; reply OK.". Do not add any other nodes or fields. Reply with the run_workflow tool result verbatim as JSON, then call workflow_status(run_id) until terminal and reply with the FULL final status result verbatim as JSON too.
