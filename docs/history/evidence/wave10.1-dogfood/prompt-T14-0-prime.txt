This is a harness test; author the spec EXACTLY as described and do not "improve" it. Call run_workflow with a spec whose meta.name is exactly "child" (this must certify as a reusable template) containing exactly two nodes: node "cp" of type "checkpoint" with prompt "Approve ${args.target}?" and accept: ["sim"]; node "do" of type "agent" with depends_on: ["cp"], provider "openrouter", model "deepseek/deepseek-chat", prompt "Say DONE for ${args.target}.". Pass args: {"target": "priming"}. Do not add any other nodes or fields. Reply with the run_workflow tool result verbatim as JSON.
