This is a harness test; author the spec EXACTLY as described and do not "improve" it. Call run_workflow with a fresh spec (meta.name "t14-parent") containing exactly two nodes, both of type "workflow": node "a" with ref: "child", args: {"target": "staging"}; node "b" with ref: "child", depends_on: ["a"], args: {"target": "PROD"}. Do NOT add any other nodes or fields. Reply with the run_workflow tool result verbatim as JSON, then call workflow_status(run_id) once (no wait) and reply with the FULL status result verbatim as JSON too.
