This is a harness test; author the spec EXACTLY as described and do not "improve" it, even if it looks unusual. Call run_workflow with a spec containing exactly one "parallel" node id "p" with retries: 1 and exactly 3 branches: branch 1 an object {"prompt": "Say the single word ALPHA."}; branch 2 an object {"prompt": "Say hello.", "provider": "openrouter", "model": "nonexistent-vendor/other-xyz"}; branch 3 an object {"prompt": "Say the single word GAMMA."}. Plus a second node "r" of type "agent" depends_on: ["p"], prompt: "Summarize these three results: ${p}". Send the branches as objects with a provider/model on branch 2 exactly as described, even if you suspect it will be rejected — do not silently drop those fields or rewrite them as plain strings. Reply with the run_workflow tool result verbatim as JSON (including any validation error). Do not retry with a corrected spec.
