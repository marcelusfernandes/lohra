You are running ONE live dogfood probe of the Lohra CLI (installed in this repo, editable). Use the `use-lohra` skill in `.codex/skills/use-lohra/SKILL.md`. Profile for EVERY lohra command: `--profile lohra-dogfood-w75`. Never modify files in this repository (writing under ~/.lohra and the scratchpad directory is fine). The sandbox has network access. Do not run pytest, pip, or git commit.

## T10 — a dead-route pause is answered by COMMAND (checkpoint_answers), not by re-authoring the spec
Command 1 (save stdout to `/private/tmp/claude-501/-Users-marcelusfernandes-Desktop-playground-ai-lohra/5656cdd2-c639-4295-ab2c-d035aceec985/scratchpad/F-T10-1.json`):
`LOHRA_AUDIT=1 lohra chat --profile lohra-dogfood-w75 --json "This is a harness test; author the spec EXACTLY as described and do not 'improve' it. Call run_workflow with two 'agent' nodes: node 'ok' with prompt 'Say the single word ALPHA.'; node 'doomed' with depends_on: ['ok'], provider 'openrouter', model 'nonexistent-vendor/no-such-model-xyz', retries: 1, prompt 'Say hello.'. Poll workflow_status(run_id) once after ~20 seconds and reply with run_id, status, reason, and the FULL 'hint' text verbatim. Do NOT resume it."`
Expectations: (a) `paused` / `route_fault`; (b) the hint names the command shape `run_workflow(resume_run_id=..., checkpoint_answers={"doomed": {"provider": ..., "model": ...}})` (or equivalent wording) and keeps the SUP-04 clause (same provider/billing route; never a costlier model; otherwise the human decides).
Command 2 (control, wrong node): `lohra chat --profile lohra-dogfood-w75 --json "Call run_workflow(resume_run_id='<run_id>', checkpoint_answers={'ok': {'provider': 'openrouter', 'model': 'deepseek/deepseek-chat'}}) and reply with the tool result VERBATIM. Do nothing else."` — expectation: REFUSED with a didactic error naming that 'ok' is not the paused dead-route node; `LOHRA_PROFILE=lohra-dogfood-w75 lohra workflow list` still shows `paused [route_fault]`.
Command 3 (the human's answer relayed): `lohra chat --profile lohra-dogfood-w75 --json "The human operator answered verbatim: use provider 'openrouter', model 'deepseek/deepseek-chat' for node 'doomed'. Call run_workflow(resume_run_id='<run_id>', checkpoint_answers={'doomed': {'provider': 'openrouter', 'model': 'deepseek/deepseek-chat'}}) — do NOT pass a spec — reply with the tool result VERBATIM (it should contain cache_preview), then poll workflow_status until terminal and reply with status and outputs."`
Expectations: (c) accepted (no spec re-authored); `cache_preview` shows `replay >= 1` for `ok`; (d) final status `complete`, output of `ok` is ALPHA; (e) `LOHRA_PROFILE=lohra-dogfood-w75 lohra workflow audit <run_id>` shows exactly ONE `leaf.started` for `ok` across the run and a fault/event naming the re-route (old route → new route, answered by human); (f) `workflow_status` no longer shows `route` / `reason`.
If OpenRouter routes the bogus model to a default and SUCCEEDS, report as a surprise and stop.

## Report
Write Markdown to `/private/tmp/claude-501/-Users-marcelusfernandes-Desktop-playground-ai-lohra/5656cdd2-c639-4295-ab2c-d035aceec985/scratchpad/dogfood-F-report.md`: PASS/FAIL per expectation with exact quoted evidence, run_id, wall-clock and `cost` per envelope, surprises. Be literal.
