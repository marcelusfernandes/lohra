You are running ONE live dogfood probe of the Lohra CLI (installed in this repo, editable). Use the `use-lohra` skill in `.codex/skills/use-lohra/SKILL.md` for the delegation shape. Profile for EVERY lohra command: `--profile lohra-dogfood-w75`. Never modify files in this repository (writing under ~/.lohra and the scratchpad directory is fine). The sandbox has network access. Do not run pytest, pip, or git commit.

## T7 — operator token-budget cap applies to a run whose spec asks for NO budget (issue #47 part 2)
Command 1 (save stdout to `/private/tmp/claude-501/-Users-marcelusfernandes-Desktop-playground-ai-lohra/5656cdd2-c639-4295-ab2c-d035aceec985/scratchpad/C-T7-1.json`):
`LOHRA_TOKEN_BUDGET_CAP=100 lohra chat --profile lohra-dogfood-w75 --json "Call run_workflow WITHOUT any token_budget argument, with a spec of ONE 'parallel' node id 'fan' with three branches, each an 'agent' prompt asking for a 200-word paragraph about a different planet. Reply with the run_workflow tool result VERBATIM as JSON (the whole dict), then call workflow_status(run_id) once (no wait) and reply with its status and reason. Do NOT resume it, do NOT re-author it, do NOT start another run."`
Expectations: (a) the run_workflow result contains `"token_budget": {"total": 100, "source": "operator_cap", "operator_cap": 100}`; (b) the run pauses with reason `token_budget_exhausted` (fan-out gate) and the envelope's top-level `workflows` has that run with `pause_reason: "token_budget_exhausted"`; (c) if the envelope or the agent's reply shows the pause hint, it must mention the OPERATOR / `LOHRA_TOKEN_BUDGET_CAP` as the remedy, not "ask a human for a bigger token_budget".
Command 2: `LOHRA_PROFILE=lohra-dogfood-w75 LOHRA_TOKEN_BUDGET_CAP=100 lohra workflow watch <run_id>` with a 60 s watchdog — expectation: exits on its own within a few seconds; the stderr line names the operator ceiling (`OPERATOR`, `LOHRA_TOKEN_BUDGET_CAP`).
Command 3 (control, no cap): `lohra chat --profile lohra-dogfood-w75 --json --no-tools "Reply with exactly PONG"` — expectation: envelope has NO `workflows` key and the reply is PONG (byte-identical default path).

## Report
Write Markdown to `/private/tmp/claude-501/-Users-marcelusfernandes-Desktop-playground-ai-lohra/5656cdd2-c639-4295-ab2c-d035aceec985/scratchpad/dogfood-C-report.md`: PASS/FAIL per expectation with exact quoted evidence (JSON fields, stderr lines), run_id, wall-clock and `cost` of each envelope, and any surprise. Be literal.
