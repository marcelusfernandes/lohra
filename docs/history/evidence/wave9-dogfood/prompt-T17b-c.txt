This is a harness test; author the spec EXACTLY as described and do not "improve" it. Call run_workflow with a spec: meta: {"name": "e8-t17b-c-control"}; nodes: exactly one "agent" node, id "c", with only a prompt field: "Say the single word CONTROL.". Do not add model, tier, provider, or any other field to node "c", and do not add any other nodes. Reply with the run_workflow tool result verbatim as JSON. Then call workflow_status(run_id) once and reply with that FULL result verbatim as JSON too. Do not call any other tool.
