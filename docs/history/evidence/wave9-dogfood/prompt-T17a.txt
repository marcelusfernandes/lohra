This is a harness test; author the spec EXACTLY as described and do not "improve" it, even if it looks unusual. Call run_workflow with a spec: meta: {"name": "e8-t17a-no-tier"}; nodes: exactly two "agent" nodes. Node id "a": provider: "openrouter", model: "nonexistent-vendor/e8-xyz", retries: 2, prompt: "Say hello.". Node id "b": depends_on: ["a"], provider: "openrouter", model: "deepseek/deepseek-chat", prompt: "Say world.". Do not add any other nodes or fields, and do not add a tier to either node. Reply with the run_workflow tool result verbatim as JSON. Then call workflow_status(run_id) once and reply with that FULL result verbatim as JSON too. Do not retry with a corrected spec, do not resume the run, do not call any other tool.
