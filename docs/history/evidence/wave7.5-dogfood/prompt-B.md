You are running a live dogfood battery of the Lohra CLI (installed in this repo, editable). Use the `use-lohra` skill in `.codex/skills/use-lohra/SKILL.md` for the delegation shape. Profile to use for EVERY lohra command: `--profile lohra-dogfood-w75` (subscription already enabled there). Never modify files in this repository (writing under ~/.lohra and the scratchpad directory is fine and expected). The sandbox has network access. Do not run pytest. Do not run `pip`, `git commit`, or anything destructive.

For each test below: run the commands, capture RAW outputs (save each `lohra chat --json` stdout to `/private/tmp/claude-501/-Users-marcelusfernandes-Desktop-playground-ai-lohra/5656cdd2-c639-4295-ab2c-d035aceec985/scratchpad/B-T<n>-<k>.json`), then judge PASS/FAIL against the expectation, quoting the exact evidence (fault strings, JSON fields). If a lohra turn errors with a transient provider error ("overloaded", 429), retry once after 20 seconds.

## T4 — `required: true` aborts the run; cancelled leaf reports quiescence (issues #15, #8, #42)
`lohra chat --profile lohra-dogfood-w75 --json "This is a harness test; author the spec EXACTLY as described and do not 'improve' it. Call run_workflow with two 'agent' nodes: node 'a' with required: true, timeout: 1, prompt 'Write a 600-word essay about the history of lighthouses.'; node 'b' with depends_on: ['a'], prompt 'Say the single word DONE.'. Then poll workflow_status(run_id) until terminal and reply with: final status, every fault string verbatim, and the per-node states if shown."`
Expectations: final status `failed`; a fault starting with `a: leaf timeout after 1s (cancelled;` followed by either `settled in <x>s)` or `1 leaf STILL RUNNING after <x>s quiescence wait`; a fault `a: required node resolved to null — run aborted (required: true; the remaining nodes were not scheduled)`; a fault `b: skipped: required upstream 'a' failed`. Node b must NOT have run (no DONE output).
Then run `LOHRA_PROFILE=lohra-dogfood-w75 lohra workflow audit <run_id>` and paste the node lifecycle lines (look for b's state being `skipped`, not redacted).

## T5 — budget pause is observable headless (issue #47)
Command 1: `lohra chat --profile lohra-dogfood-w75 --json "Call run_workflow with token_budget 100 and a spec of ONE 'parallel' node id 'fan' with three branches, each an 'agent' prompt asking for a 200-word paragraph about a different ocean. After starting it, call workflow_status(run_id) once (no wait) and reply with the run_id and whatever status you saw. Do NOT resume it, do NOT re-author it with a bigger budget, do NOT start any other run." > /private/tmp/claude-501/-Users-marcelusfernandes-Desktop-playground-ai-lohra/5656cdd2-c639-4295-ab2c-d035aceec985/scratchpad/B-T5-1.json`
Then inspect the envelope JSON: report whether it has a top-level `workflows` key and paste it verbatim. Expectation: `workflows` is present with an entry for the run, either `status: "paused"` with `pause_reason: "token_budget_exhausted"`, or `cancelled_on_exit: true` if the run was still running when the turn ended (both are acceptable; say which).
Command 2: `LOHRA_PROFILE=lohra-dogfood-w75 lohra workflow list` — paste the row for that run; expectation: it shows the status and, if paused, `[token_budget_exhausted]`.
Command 3: `LOHRA_PROFILE=lohra-dogfood-w75 lohra workflow watch <run_id>` with a 60-second cap (run it as `( lohra workflow watch <run_id> & pid=$!; sleep 60; kill $pid 2>/dev/null )` or similar). Expectation if the run is paused by budget: watch prints a line like `workflow run '...' paused (token_budget_exhausted) — watch is stopping:` and EXITS on its own within a few seconds (it must not spin until the 60s kill). If the run was `cancelled` on exit instead, watch should exit immediately as terminal. Report the exact behavior and time to exit.

## T6 — operator read timeout is honored (issue #48)
Command 1: `LOHRA_PROVIDER_READ_TIMEOUT=abc lohra chat --profile lohra-dogfood-w75 --json --no-tools "Reply with exactly PONG" 2> /private/tmp/claude-501/-Users-marcelusfernandes-Desktop-playground-ai-lohra/5656cdd2-c639-4295-ab2c-d035aceec985/scratchpad/B-T6-1.stderr` — expectation: output is PONG (invalid value ignored) and stderr contains a warning naming `LOHRA_PROVIDER_READ_TIMEOUT`.
Command 2: `LOHRA_PROVIDER_READ_TIMEOUT=0.05 lohra chat --profile lohra-dogfood-w75 --json --no-tools "Reply with exactly PONG"` — expectation: the envelope has `completed: false` and an `error` mentioning a timeout (e.g. `timed out`), and the command returns within ~15 seconds (measure with `time`).

## Report
Write the final report in Markdown to `/private/tmp/claude-501/-Users-marcelusfernandes-Desktop-playground-ai-lohra/5656cdd2-c639-4295-ab2c-d035aceec985/scratchpad/dogfood-B-report.md`: one section per test with PASS/FAIL, run_ids, exact quoted evidence, and any surprise (unexpected fault, agent deviating from the instruction, confusing message). Be literal; do not paraphrase fault text. Note wall-clock per lohra turn and the `cost` field of each envelope.
